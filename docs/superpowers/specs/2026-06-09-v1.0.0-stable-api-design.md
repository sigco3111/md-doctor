# v1.0.0 — 안정 API + 정확도 벤치마크 + 문서 설계

**날짜**: 2026-06-09
**상태**: 초안 (설계 단계)
**대상 버전**: md-doctor 1.0.0
**스코프**: 안정 API 마커, API 레퍼런스, 정확도 벤치마크 (5개 픽스처)

---

## 1. 배경과 목표

### 1.1 현재 상태 (v0.5.0)

- 5개 활성 검사 (token-stats, heading-consistency, broken-images, dead-links, gfm-lint)
- 1개 opt-in (korean-grammar)
- 1개 subcommand/flag (--fix)
- 119개 테스트 통과
- zero-deps 유지
- README 로드맵: "1.0.0 — 안정 API + 정확도 벤치마크 + PyPI 정식 배포"

### 1.2 목표

| 목표 | 측정 |
|------|------|
| 안정 API 명시 | docstring `.. versionadded::` + `.. stability:: stable` |
| API 레퍼런스 | `docs/superpowers/api.md` (수동) |
| 정확도 벤치마크 | 5개 픽스처 + recall/precision 측정 |
| zero-deps 유지 | 외부 라이브러리 추가 없음 |
| backward compatible | v0.5.0 → v1.0.0 시 API 변경 0건 (마커만 추가) |
| TDD 적용 | RED-GREEN + 5+ 신규 시나리오 |

### 1.3 비목표 (Out of scope)

- **PyPI 실제 배포** (1.0.0+ 후속)
- **mypy strict** (점진적)
- **GitHub Actions matrix** (Linux/macOS/Windows) — v1.0.1+
- **docs 자동 생성 (sphinx, mkdocs)** — 의존성 추가 회피
- **Performance 회귀 가드** — v1.0.1+
- **API 변경** (signature, 동작) — v1.0.0 은 마커만

---

## 2. 아키텍처

### 2.1 모듈 구조

```
tests/
├── fixtures/benchmark/         # [신규] 5개 픽스처
│   ├── sample_clean.md
│   ├── sample_broken_images.md
│   ├── sample_broken_links.md
│   ├── sample_korean.md
│   └── sample_html_table.md
├── test_benchmark.py            # [신규] 정확도 측정
docs/superpowers/
└── api.md                       # [신규] API 레퍼런스 (수동)
```

### 2.2 안정 API 마커

docstring 에 Sphinx-style 마커 사용:

```python
def diagnose_file(path, *, registry=None):
    """한 마크다운 파일을 진단.
    
    .. versionadded:: 0.1.0
    .. versionchanged:: 0.3.0  --registry 추가
    .. stability:: stable
    """
```

**카테고리**:
- `stable`: 1.0.0 이후 시맨틱 버전 유지, BACKWARD COMPATIBLE 보장
- `public`: 노출되지만 breaking change 가능 (v0.5.0 → v1.0.0 의 모든 API)
- `internal`: 외부 노출 비권장

### 2.3 API 레퍼런스 (`docs/superpowers/api.md`)

수동 작성. 각 모듈의 공개 인터페이스 정리:

```markdown
# md-doctor 1.0.0 — Public API Reference

## Core
- `diagnose_file(path, *, registry=None) -> FileReport`
- `diagnose_tree(root, *, glob='**/*.md', registry=None) -> DoctorReport`
- `iter_diagnoses(reports, *, min_severity=Severity.INFO) -> Iterator[Diagnosis]`
- `filter_diagnoses(reports, *, checks=None, min_severity=Severity.INFO) -> list[Diagnosis]`

## Models
- `Severity` (enum: INFO, WARNING, ERROR)
- `Diagnosis` (frozen dataclass)
- `FileReport`, `DoctorReport`

## Extractors
- `extract_image_refs(text, lines) -> list[ImageRef]`
- `extract_link_refs(text, lines) -> list[LinkRef]`
- `extract_html_refs(text, lines, fences) -> list[HtmlRef]`
- `mark_code_regions(lines) -> list[CodeFence]`
- `strip_inline_code(line) -> str`
- `is_in_code_region(line_no, fences) -> bool`

## Fixers (v0.4.0+)
- `apply_fixes(text, *, enabled=None) -> FixResult`
- `FixResult` (frozen dataclass)
- `ALL_TRANSFORMS` (list of (id, transform) tuples)

## CLI
- `md-doctor <path>` (diagnose, default)
- `md-doctor --fix <path>` (v0.4.0+)
- `md-doctor --korean <path>` (v0.5.0+)
- Flags: --format {text,json,github}, --min-severity, --fail-on, --checks, --backup, --no-backup, --dry-run
```

### 2.4 정확도 벤치마크 (`tests/test_benchmark.py`)

5개 픽스처에 대해 recall/precision/F1 측정:

```python
def _expected_diagnoses(fixture_name: str) -> list[dict]:
    """수동 정의된 기대 진단."""
    return {
        "sample_clean.md": [],
        "sample_broken_images.md": [
            {"check": "broken-images", "rule": "BI001", "line": 3},
            {"check": "broken-images", "rule": "BI001", "line": 5},
        ],
        # ...
    }.get(fixture_name, [])


def test_benchmark_sample_clean():
    fr = diagnose_file(FIXTURES / "sample_clean.md")
    actual = [(d.check, d.rule, d.line) for d in fr.diagnoses]
    expected = _expected_diagnoses("sample_clean.md")
    precision, recall, f1 = _compute_metrics(actual, expected)
    assert recall == 1.0  # 모든 기대 진단 검출
    # (precision 은 false positive 허용 가능)
```

---

## 3. 5개 픽스처

### 3.1 `sample_clean.md`

```markdown
# 깨끗한 문서

이 문서는 정상입니다. 모든 검사를 통과합니다.

- 항목 1
- 항목 2

| a | b |
|---|---|
| 1 | 2 |
```

기대: 0건 (또는 INFO 무시, ERROR/WARNING 0).

### 3.2 `sample_broken_images.md`

```markdown
# 깨진 이미지

![정상](ok.png)

![깨짐 1](missing1.png)

![깨짐 2](missing_dir/missing2.png)
```

기대: `broken-images` BI001 × 2.

### 3.3 `sample_broken_links.md`

```markdown
# 깨진 링크

[정상](https://github.com)

[깨진 로컬](missing.md)

[깨진 외부](https://missing.example.com)
```

기대: `dead-links` DL001 × 2 (mock 불필요, mock 가능).

### 3.4 `sample_korean.md`

```markdown
# 한국어 위반

Python은 좋은 언어다.
中文언어도 배운다.
10 개 있다.
```

기대: korean-grammar KS1, KS2, KS3 (--korean 모드).

### 3.5 `sample_html_table.md`

```markdown
# HTML + 테이블

<iframe src="evil"></iframe>

| a | b | c |
|---|---|
| 1 | 2 | 3 |
```

기대: gfm-lint HC007 (iframe), HC008 (테이블 정렬).

---

## 4. 정확도 측정 방식

### 4.1 `precision/recall/F1`

```python
def _compute_metrics(actual, expected):
    actual_set = set(tuple(sorted(d.items())) for d in actual)
    expected_set = set(tuple(sorted(d.items())) for d in expected)
    
    if not actual_set and not expected_set:
        return 1.0, 1.0, 1.0
    
    tp = len(actual_set & expected_set)
    fp = len(actual_set - expected_set)
    fn = len(expected_set - actual_set)
    
    precision = tp / (tp + fp) if (tp + fp) else 0.0
    recall = tp / (tp + fn) if (tp + fn) else 0.0
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) else 0.0
    
    return precision, recall, f1
```

### 4.2 임계값

- **recall ≥ 0.9** (각 픽스처)
- **precision ≥ 0.8** (false positive 어느 정도 허용)
- **F1 ≥ 0.85**

### 4.3 픽스처별 차이

- 깨끗한 파일: precision 0 도 OK (recall 만 1.0)
- 위반 파일: precision ≥ 0.5 (보수적), recall ≥ 0.9

---

## 5. 시나리오 / 계약 (TDD 입력)

### BM_T1. 깨끗한 픽스처
```python
def test_benchmark_clean_zero_diagnoses():
    fr = diagnose_file(FIXTURES / "sample_clean.md")
    # ERROR/WARNING 0
    assert fr.total_errors == 0
    assert fr.total_warnings == 0
```

### BM_T2. 깨진 이미지 recall
```python
def test_benchmark_broken_images_recall():
    fr = diagnose_file(FIXTURES / "sample_broken_images.md")
    rules = [d.rule for d in fr.diagnoses if d.check == "broken-images"]
    assert rules.count("BI001") == 2  # 2개 모두 검출
```

### BM_T3. 깨진 링크 recall (mock 사용)
```python
def test_benchmark_broken_links_recall(monkeypatch):
    monkeypatch.setattr(
        "md_doctor.checks.dead_links.head_request", lambda url, timeout=5.0: 404
    )
    fr = diagnose_file(FIXTURES / "sample_broken_links.md")
    # 정확한 F1 측정
    ...
```

### BM_T4. 한국어 recall
```python
def test_benchmark_korean_recall():
    registry = CheckRegistry(checks=[KoreanGrammarCheck()])
    fr = diagnose_file(FIXTURES / "sample_korean.md", registry=registry)
    rules = {d.rule for d in fr.diagnoses}
    assert "KS1" in rules
    assert "KS2" in rules
    assert "KS3" in rules
```

### BM_T5. HTML+테이블 recall
```python
def test_benchmark_html_table_recall():
    fr = diagnose_file(FIXTURES / "sample_html_table.md")
    rules = {d.rule for d in fr.diagnoses}
    assert "HC007" in rules
    assert "HC008" in rules
```

### BM_T6. 종합 정확도 보고
```python
def test_benchmark_overall_metrics():
    """전체 픽스처의 평균 F1 ≥ 0.85."""
    f1s = []
    for name in FIXTURE_NAMES:
        # ... 각 픽스처 F1 계산
        f1s.append(f1)
    assert sum(f1s) / len(f1s) >= 0.85
```

---

## 6. 호환성 / 마이그레이션

### 6.1 API
- **변경 0건** — v0.5.0 → v1.0.0 시 시그니처 동일
- **추가**: docstring 마커 (호환성 영향 없음)
- **추가**: `docs/superpowers/api.md` (문서)
- **추가**: `tests/test_benchmark.py` (테스트)

### 6.2 CLI
- 모든 v0.5.0 옵션 100% 보존
- 신규 옵션 없음

### 6.3 CHANGELOG
- v1.0.0 섹션 추가 (Added/Notes)
- `## [0.5.0] - 2026-06-09` → v1.0.0 의 직전 버전

### 6.4 README
- "1.0.0 출시" 섹션 (안정 API + 정확도 벤치마크)
- 로드맵 1.0.0 → ✅
- "PyPI 정식 배포" 향후 항목 (1.0.1+)

---

## 7. 테스트 전략

| 파일 | 신규 케이스 |
|------|---------|
| `tests/fixtures/benchmark/*.md` (5개) | 픽스처 |
| `tests/test_benchmark.py` | 6+ |
| `docs/superpowers/api.md` | (문서) |

총 +6 신규 테스트. 기존 119 + 6 = **125개** 목표.

---

## 8. 결정 기록 (Decisions)

| # | 결정 | 이유 |
|---|------|------|
| D1 | docstring 기반 API 마커 | 도구 의존 X, 단순 |
| D2 | API 레퍼런스 수동 (api.md) | sphinx/mkdocs 의존 회피 |
| D3 | 5개 픽스처 = 내장 (tests/fixtures/) | 외부 의존 회피 |
| D4 | 정확도 측정 = rule ID + severity + line | 명확한 비교 |
| D5 | zero-deps 유지 | 정책 일관 |
| D6 | PyPI 골격 보류 | v1.0.0+ 후속 |
| D7 | API 변경 0건 (마커만) | 1.0.0 의 약속 |
| D8 | recall ≥ 0.9, precision ≥ 0.8, F1 ≥ 0.85 | 현실적 임계값 |

---

## 9. 참고 자료

- v0.5.0 spec: `docs/superpowers/specs/2026-06-09-v0.5.0-korean-grammar-design.md`
- Sphinx docstring conventions: https://www.sphinx-doc.org/en/master/usage/extensions/autodoc.html
- Semantic Versioning 2.0.0: https://semver.org/
- TREC-style precision/recall: https://en.wikipedia.org/wiki/Precision_and_recall

---

## 10. 승인 체크리스트

- [x] 사용자: 작업 범위 = 안정 API + 문서 (PyPI 보류)
- [x] 사용자: 디자인 방향 = 5개 픽스처 + 수동 api.md
- [ ] writing-plans 스킬로 구현 계획 작성
- [ ] 5개 픽스처 작성
- [ ] 벤치마크 테스트 작성
- [ ] API 레퍼런스 작성
- [ ] 수동 QA + CHANGELOG/README 갱신
- [ ] 커밋 + 푸시
