# v0.5.0 --korean opt-in — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `md-doctor --korean` opt-in 플래그로 한국어 띄어쓰기/맞춤법 검사 (KS1-KS5 5개 규칙) 를 추가한다. zero-deps 정책 유지, 기본 비활성.

**Architecture:** 단일 체크 `KoreanGrammarCheck` (KS1-KS5 5개 규칙) 신규. 정규식 기반, 코드 영역 무시. CLI `--korean` 플래그가 있을 때 `default_checks()` registry 에 자동 추가. `default_checks()` 자체는 변경 없음 (opt-in).

**Tech Stack:** Python 3.9+ stdlib only (zero-deps), pytest, re. 외부 라이브러리 추가 없음.

**Spec:** `docs/superpowers/specs/2026-06-09-v0.5.0-korean-grammar-design.md`

**저장소:** `/Users/hjshin/Desktop/project/work/ai-driven-dev/md-doctor`

---

## 파일 구조 (변경 요약)

| 작업 | 파일 | 책임 |
|------|------|------|
| Create | `src/md_doctor/checks/korean_grammar.py` | `KoreanGrammarCheck` (KS1-5) |
| Modify | `src/md_doctor/cli.py` | `--korean` 플래그 + registry 자동 추가 |
| Modify | `pyproject.toml` | version `0.4.0` → `0.5.0` |
| Modify | `CHANGELOG.md` | v0.5.0 섹션 |
| Modify | `README.md` | `--korean` 사용 예 + 로드맵 |
| Modify | `src/md_doctor/__init__.py` | `__version__` |
| Modify | `tests/test_cli.py` | 버전 어설션 (3개) + CLI 2개 |
| Create | `tests/test_korean_grammar.py` | 10 케이스 |

총 변경: 신규 2, 수정 5. 기존 107 + 12 = **119개** 목표.

---

## Task 1: `korean_grammar.py` RED 테스트

**Files:**
- Create: `tests/test_korean_grammar.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
"""KoreanGrammarCheck 단위 테스트 — KS1~KS5 5개 규칙."""

from __future__ import annotations

from pathlib import Path

import pytest

from md_doctor import Severity
from md_doctor.checks.korean_grammar import KoreanGrammarCheck
from md_doctor.core import diagnose_file


@pytest.fixture
def kor_check():
    return KoreanGrammarCheck()


def _run(lines: list[str]) -> list:
    """테스트 헬퍼: lines 를 진단하고 korean-grammar 결과만 반환."""
    from pathlib import Path
    import tempfile
    with tempfile.NamedTemporaryFile(mode="w", suffix=".md", delete=False) as f:
        f.write("\n".join(lines))
        f.flush()
        fr = diagnose_file(Path(f.name))
    return [d for d in fr.diagnoses if d.check == "korean-grammar"]


def test_ks1_english_korean_no_space():
    """KS1: 영어+한글 띄어쓰기 권장."""
    diags = _run(["Python은 좋은 언어다"])
    ks1 = [d for d in diags if d.rule == "KS1"]
    assert len(ks1) == 1
    assert ks1[0].severity is Severity.INFO


def test_ks2_hanja_korean_no_space():
    """KS2: 한자+한글 띄어쓰기 권장."""
    diags = _run(["中文언어를 배운다"])
    ks2 = [d for d in diags if d.rule == "KS2"]
    assert len(ks2) == 1


def test_ks3_number_korean_unit_with_space():
    """KS3: 숫자+단위 사이 띄어쓰기 위반 (WARNING)."""
    diags = _run(["10 개 있다"])
    ks3 = [d for d in diags if d.rule == "KS3"]
    assert len(ks3) == 1
    assert ks3[0].severity is Severity.WARNING


def test_ks4_double_space():
    """KS4: 중복 공백."""
    diags = _run(["a  b"])
    ks4 = [d for d in diags if d.rule == "KS4"]
    assert len(ks4) == 1
    assert ks4[0].severity is Severity.INFO


def test_ks5_three_dots():
    """KS5: '...' → '…' 권장."""
    diags = _run(["생각..."])
    ks5 = [d for d in diags if d.rule == "KS5"]
    assert len(ks5) == 1
    assert ks5[0].severity is Severity.INFO


def test_normal_korean_no_diagnosis():
    """정상 문장 → 0건."""
    diags = _run(["한글은 좋습니다. English 도 좋습니다."])
    assert all(d.rule not in ("KS1", "KS2", "KS3", "KS4", "KS5") for d in diags)


def test_code_fence_ignored():
    """코드펜스 내부 무시."""
    diags = _run(["```", "Python은", "```"])
    assert diags == []


def test_inline_code_ignored():
    """인라인 코드 내부 무시."""
    diags = _run(["use `Python은` here"])
    assert diags == []


def test_combined():
    """복합 (KS1+KS3+KS5 동시)."""
    diags = _run(["Python은 좋다. 10 개 있다. ..."])
    rules = {d.rule for d in diags}
    assert "KS1" in rules
    assert "KS3" in rules
    assert "KS5" in rules


def test_empty_no_diagnosis():
    """빈 파일 → 0건."""
    diags = _run([""])
    assert diags == []
```

- [ ] **Step 2: RED 확인**

```bash
pytest tests/test_korean_grammar.py -v
```

Expected: `ModuleNotFoundError: No module named 'md_doctor.checks.korean_grammar'`.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_korean_grammar.py
git commit -m "test(korean): 5 규칙 실패 테스트 추가 (RED, 10 케이스)"
```

---

## Task 2: `korean_grammar.py` 구현 (GREEN)

**Files:**
- Create: `src/md_doctor/checks/korean_grammar.py`

- [ ] **Step 1: 모듈 작성**

```python
"""한국어 띄어쓰기/맞춤법 검사 — KS1~KS5 5개 규칙 (opt-in).

zero-deps, 정규식 기반. ``--korean`` 플래그 시에만 활성.
"""

from __future__ import annotations

import re
from collections.abc import Iterator
from typing import Any

from md_doctor.checks import BaseCheck
from md_doctor.extractors.code_fence import is_in_code_region, mark_code_regions
from md_doctor.models import Diagnosis, Severity


# KS1: 영어 단어 + 한글 (lookahead 로 다음 한글 1자 확인)
_KS1_RE = re.compile(r"[A-Za-z]+(?=[가-힣])")

# KS2: 한자 + 한글
_KS2_RE = re.compile(r"[一-鿿㐀-䶿]+(?=[가-힣])")

# KS3: 숫자 + 공백 + 한글
_KS3_RE = re.compile(r"\d+\s+[가-힣]+")

# KS4: 2+ 연속 공백
_KS4_RE = re.compile(r"  +")

# KS5: 3+ 연속 마침표
_KS5_RE = re.compile(r"\.{3,}")


class KoreanGrammarCheck(BaseCheck):
    """한국어 띄어쓰기/맞춤법 검사 (KS1~KS5)."""

    name = "korean-grammar"

    def run(self, context: dict[str, Any]) -> Iterator[Diagnosis]:
        lines: list[str] = context["lines"]
        fences = mark_code_regions(lines)
        for i, line in enumerate(lines, start=1):
            if is_in_code_region(i, fences):
                continue
            yield from self._ks1(line, i)
            yield from self._ks2(line, i)
            yield from self._ks3(line, i)
            yield from self._ks4(line, i)
            yield from self._ks5(line, i)

    @staticmethod
    def _ks1(line: str, line_no: int) -> Iterator[Diagnosis]:
        for m in _KS1_RE.finditer(line):
            word = m.group(0)
            idx = m.end()
            next_char = line[idx] if idx < len(line) else ""
            yield Diagnosis(
                check="korean-grammar",
                severity=Severity.INFO,
                message=f"영어 단어와 한글 사이 띄어쓰기 권장: '{word}{next_char}'",
                line=line_no,
                rule="KS1",
            )

    @staticmethod
    def _ks2(line: str, line_no: int) -> Iterator[Diagnosis]:
        for m in _KS2_RE.finditer(line):
            word = m.group(0)
            idx = m.end()
            next_char = line[idx] if idx < len(line) else ""
            yield Diagnosis(
                check="korean-grammar",
                severity=Severity.INFO,
                message=f"한자와 한글 사이 띄어쓰기 권장: '{word}{next_char}'",
                line=line_no,
                rule="KS2",
            )

    @staticmethod
    def _ks3(line: str, line_no: int) -> Iterator[Diagnosis]:
        for m in _KS3_RE.finditer(line):
            yield Diagnosis(
                check="korean-grammar",
                severity=Severity.WARNING,
                message=f"숫자와 한글 단위 사이 띄어쓰기 없음: '{m.group(0).strip()}'",
                line=line_no,
                rule="KS3",
            )

    @staticmethod
    def _ks4(line: str, line_no: int) -> Iterator[Diagnosis]:
        if _KS4_RE.search(line):
            yield Diagnosis(
                check="korean-grammar",
                severity=Severity.INFO,
                message="중복 공백 발견",
                line=line_no,
                rule="KS4",
            )

    @staticmethod
    def _ks5(line: str, line_no: int) -> Iterator[Diagnosis]:
        for m in _KS5_RE.finditer(line):
            yield Diagnosis(
                check="korean-grammar",
                severity=Severity.INFO,
                message="'...' 대신 '…' 권장",
                line=line_no,
                rule="KS5",
            )
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_korean_grammar.py -v
```

Expected: **10 passed**.

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/checks/korean_grammar.py
git commit -m "feat(korean): KoreanGrammarCheck 구현 (GREEN, 10 passed)"
```

---

## Task 3: CLI `--korean` 플래그

**Files:**
- Modify: `src/md_doctor/cli.py`

- [ ] **Step 1: argparse 추가 + dispatch**

기존 `_build_parser()` 에:

```python
parser.add_argument(
    "--korean",
    action="store_true",
    help="한국어 띄어쓰기/맞춤법 검사 opt-in (KS1~KS5)",
)
```

`main()` 에서 `--korean` 플래그 dispatch:

```python
def main(argv=None):
    parser = _build_parser()
    args = parser.parse_args(argv)

    if args.fix:
        return _run_fix(args)

    if args.list_checks:
        ...

    path = args.path
    ...
    registry = _filter_registry(checks)

    # --korean opt-in: korean-grammar 자동 추가
    if args.korean:
        from md_doctor.checks.korean_grammar import KoreanGrammarCheck
        if not any(c.name == "korean-grammar" for c in registry.checks):
            registry.checks.append(KoreanGrammarCheck())

    # 기존 diagnose 로직
    ...
```

- [ ] **Step 2: 회귀**

```bash
pytest tests/test_cli.py -v 2>&1 | tail -10
```

Expected: 기존 23개 모두 통과 (--korean 추가만, 기존 diagnose 영향 없음).

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/cli.py
git commit -m "feat(cli): --korean opt-in 플래그 추가 (korean-grammar 자동 활성화)"
```

---

## Task 4: CLI 통합 테스트 2개 (RED+GREEN, --korean 토글)

**Files:**
- Modify: `tests/test_cli.py` (append)

- [ ] **Step 1: 테스트 추가**

```python
# korean-grammar CLI 통합 ----------------------------------------------


def test_cli_no_korean_flag_no_check(tmp_path: Path, capsys):
    """--korean 없으면 korean-grammar 비활성."""
    import json as _json
    md = tmp_path / "test.md"
    md.write_text("Python은 좋다", encoding="utf-8")
    rc = main([str(md), "--format", "json"])
    assert rc == 0
    payload = _json.loads(capsys.readouterr().out)
    korean = [
        d
        for f in payload["files"]
        for d in f["diagnoses"]
        if d["check"] == "korean-grammar"
    ]
    assert korean == []


def test_cli_with_korean_flag_active(tmp_path: Path, capsys):
    """--korean 있으면 korean-grammar 활성."""
    import json as _json
    md = tmp_path / "test.md"
    md.write_text("Python은 좋다", encoding="utf-8")
    rc = main([str(md), "--korean", "--format", "json"])
    assert rc == 0
    payload = _json.loads(capsys.readouterr().out)
    korean = [
        d
        for f in payload["files"]
        for d in f["diagnoses"]
        if d["check"] == "korean-grammar"
    ]
    assert len(korean) >= 1
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_cli.py -v -k "korean"
```

Expected: 2 passed.

- [ ] **Step 3: 회귀 (전체)**

```bash
pytest 2>&1 | tail -3
```

Expected: **119 passed** (107 + 10 + 2).

- [ ] **Step 4: 커밋**

```bash
git add tests/test_cli.py
git commit -m "test(cli): --korean opt-in CLI 통합 테스트 2개 추가 (119 passed)"
```

---

## Task 5: 수동 QA

- [ ] **Step 1: README 한국어 검사**

```bash
cd /Users/hjshin/Desktop/project/work/ai-driven-dev/md-doctor

python -m md_doctor --korean README.md --format text 2>&1 | head -30
```

Expected: KS1 (영어+한글) 등 발견.

- [ ] **Step 2: 깨끗한 한국어**

```bash
cat > /tmp/qa_kor_clean.md <<'EOF'
# 깨끗한 문서

한글은 좋습니다. English 도 좋습니다. 10개 있다.
EOF
python -m md_doctor --korean /tmp/qa_kor_clean.md --format text 2>&1
rm -f /tmp/qa_kor_clean.md
```

Expected: "표시할 이슈가 없습니다".

- [ ] **Step 3: 다양한 위반**

```bash
cat > /tmp/qa_kor_bad.md <<'EOF'
# 위반

Python은 좋은 언어다.
中文언어도 배운다.
10 개 있다.
생각...
EOF
python -m md_doctor --korean /tmp/qa_kor_bad.md --format text 2>&1
rm -f /tmp/qa_kor_bad.md
```

Expected: KS1, KS2, KS3, KS5 진단.

- [ ] **Step 4: opt-out 확인**

```bash
cat > /tmp/qa_kor_optout.md <<'EOF'
Python은 좋다
EOF
python -m md_doctor /tmp/qa_kor_optout.md --format text 2>&1 | head -5
rm -f /tmp/qa_kor_optout.md
```

Expected: korean-grammar 진단 **없음** (opt-out, 기본 비활성).

- [ ] **Step 5: --list-checks 에 korean-grammar 노출 여부**

```bash
python -m md_doctor --list-checks
```

Expected: korean-grammar **없음** (opt-in, default_checks 미포함). 또는 표시되어도 opt-out.

---

## Task 6: CHANGELOG + README + version

- [ ] **Step 1: CHANGELOG.md v0.5.0 섹션**

```markdown
## [0.5.0] - 2026-06-09

### Added
- 🇰🇷 `--korean` opt-in 플래그 — 한국어 띄어쓰기/맞춤법 검사 (KS1~KS5)
  - KS1: 영어 단어 + 한글 사이 띄어쓰기 권장
  - KS2: 한자 + 한글 사이 띄어쓰기 권장
  - KS3: 숫자 + 한글 단위 사이 띄어쓰기 없음 (WARNING)
  - KS4: 중복 공백 발견
  - KS5: `...` → `…` 권장
- 🛡️ zero-deps 유지 (정규식 기반, 외부 사전 미사용)
- ✅ 12 신규 테스트 (korean_grammar 10 + CLI 2), 총 119개

### Notes
- 기본 비활성 — `--korean` 명시 시에만 활성.
- 다른 검사와 자동 결합 (--korean README.md 만으로 가능).
- zero-deps 정책 유지.

## [0.4.0] - 2026-06-09
```

- [ ] **Step 2: README 사용 예 추가**

```bash
# 한국어 검사 (opt-in)
md-doctor --korean README.md
md-doctor --korean README.md --format json
```

- [ ] **Step 3: README 로드맵 0.5.0 → ✅**

```
- **0.4.0** ✅ — md-doctor --fix (6개 안전 변환)
- **0.5.0** ✅ — --korean opt-in (KS1-5, zero-deps)
```

- [ ] **Step 4: version bump**

- [ ] **Step 5: 회귀 + 커밋**

```bash
pytest 2>&1 | tail -3
git add CHANGELOG.md README.md pyproject.toml src/md_doctor/__init__.py tests/test_cli.py
git commit -m "docs: v0.5.0 CHANGELOG/README 갱신 + version bump (0.4.0 → 0.5.0)"
```

---

## Task 7: 최종 검증

- [ ] **Step 1: 전체 테스트**

```bash
pytest -v 2>&1 | tail -10
```

Expected: **119 passed**.

- [ ] **Step 2: ruff 신규 파일**

```bash
ruff check src/md_doctor/checks/korean_grammar.py tests/test_korean_grammar.py
```

- [ ] **Step 3: zero-deps**

```bash
grep -A 1 "^dependencies" pyproject.toml
```

- [ ] **Step 4: CLI**

```bash
python -m md_doctor --version
python -m md_doctor --help | grep korean
```

- [ ] **Step 5: git status**

```bash
git status
```

---

## 변경 요약 (전체)

| Task | 신규 | 수정 | 테스트 추가 |
|------|------|------|------------|
| 1-2 | 1 (test) + 1 (impl) | - | +10 |
| 3 | - | 1 (cli) | 0 |
| 4 | - | 1 (test_cli) | +2 |
| 5 | - | (수동) | 0 |
| 6 | - | 5 (docs+version+test) | 0 |
| 7 | - | (검증) | 0 |

**총 커밋**: 7
**최종 테스트**: 119 passed (기존 107 + 신규 12)

---

## 위험 & 롤백

- **Task 3 호환성**: --korean 추가만, 기존 diagnose 로직 무수정. 회귀 23개 통과로 확인.
- **KS3 false positive**: 단순 정규식 (\d + 공백 + 한글) — "1 0명" 같은 한글 단위가 아닌 경우도 매칭. **v0.5.0 범위 단순화**, v0.5.1+ 정밀화.
- **KS1/KS2 false positive**: 한국어 텍스트에서 흔한 일부 패턴 ("theApple" 등 영문 약어 + 한글) 도 매칭. 사용자 검토 필요.

롤백:
```bash
git log --oneline -10
git reset --hard <last-green-commit>
```
