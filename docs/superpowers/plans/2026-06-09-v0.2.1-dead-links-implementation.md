# v0.2.1 dead-links 활성화 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `DeadLinksCheck` 를 스텁에서 활성 검사 모듈로 변환하여, 마크다운의 `[text](url)` 참조 중 (1) 로컬 파일이 존재하지 않거나 (2) 외부 URL 의 HTTP status 가 4xx/5xx/타임아웃일 때 ERROR 진단을 생성한다. zero-deps 정책과 오프라인 우선 원칙을 유지한다 (외부 검사는 mockable).

**Architecture:** v0.2.0 과 동일한 단일 책임 분리 패턴: **추출기**(`extractors/links.py`, 순수 함수 — 텍스트 → `LinkRef` 리스트)와 **체크**(`checks/dead_links.py`, 베이스 클래스 — 추출 결과 → 진단)를 분리. HTTP 요청은 `urllib.request` (stdlib only) 로 `head_request()` 헬퍼에 격리하여 mock 가능. 인라인/참조/단축/자동 링크 + 정의 테이블 처리. 기존 `core` / `cli` / `models` 는 변경하지 않으며, `checks/__init__.py` 의 `default_checks()` 에 `DeadLinksCheck()` 를 추가 등록한다.

**Tech Stack:** Python 3.9+ stdlib only (zero-deps), pytest, monkeypatch (stdlib unittest.mock 의 pytest wrapper), urllib.request, pathlib, re. 외부 라이브러리 추가 없음.

**Spec:** `docs/superpowers/specs/2026-06-09-v0.2.1-dead-links-design.md`
**v0.2.0 참고 패턴:** `docs/superpowers/specs/2026-06-09-v0.2.0-broken-images-design.md`

**저장소:** `/Users/hjshin/Desktop/project/work/ai-driven-dev/md-doctor`

---

## 파일 구조 (변경 요약)

| 작업 | 파일 | 책임 |
|------|------|------|
| Modify | `src/md_doctor/extractors/__init__.py` | `LinkRef`, `extract_link_refs` re-export 추가 |
| Create | `src/md_doctor/extractors/links.py` | `LinkRef` 데이터클래스 + `extract_link_refs()` + 정의 테이블 파서 |
| Modify | `src/md_doctor/checks/dead_links.py` | 스텁 → 실제 구현 + `head_request()` 헬퍼 |
| Modify | `src/md_doctor/checks/__init__.py` | `default_checks()` 에 `DeadLinksCheck()` 추가 |
| Modify | `pyproject.toml:7` | `version = "0.2.0"` → `0.2.1` |
| Modify | `CHANGELOG.md` | `## [0.2.1] - 2026-06-09` 섹션 추가 |
| Modify | `README.md:99` | 검사 표 `dead-links` 행 ✅ 0.2.1 로 변경 |
| Create | `tests/test_extractors_links.py` | 추출기 단위 테스트 (7개) |
| Create | `tests/test_dead_links.py` | 체크 + 통합 테스트 (8개) |
| Modify | `tests/test_cli.py` | `test_default_checks_have_unique_names` 업데이트 (3 → 4) + CLI 통합 2개 |

총 변경: 신규 3, 수정 5. 기존 41 + 신규 15 = **56개**.

---

## Task 1: `extractors/__init__.py` 에 LinkRef re-export

**Files:**
- Modify: `src/md_doctor/extractors/__init__.py`

- [ ] **Step 1: __init__.py 갱신**

기존:
```python
"""마크다운 추출기 — 텍스트에서 의미 단위(이미지/링크/표 등)를 추출.

이 모듈의 함수는 **순수** — 부수효과 없음, 디스크/네트워크 미사용.
체크 모듈은 추출기 결과를 받아 진단으로 변환한다.
"""

from md_doctor.extractors.images import ImageRef, extract_image_refs

__all__ = [
    "ImageRef",
    "extract_image_refs",
]
```

변경:
```python
"""마크다운 추출기 — 텍스트에서 의미 단위(이미지/링크/표 등)를 추출.

이 모듈의 함수는 **순수** — 부수효과 없음, 디스크/네트워크 미사용.
체크 모듈은 추출기 결과를 받아 진단으로 변환한다.
"""

from md_doctor.extractors.images import ImageRef, extract_image_refs
from md_doctor.extractors.links import LinkRef, extract_link_refs

__all__ = [
    "ImageRef",
    "extract_image_refs",
    "LinkRef",
    "extract_link_refs",
]
```

- [ ] **Step 2: 커밋**

```bash
git add src/md_doctor/extractors/__init__.py
git commit -m "feat(extractors): LinkRef/extract_link_refs re-export 추가"
```

---

## Task 2: `extract_link_refs` 실패 테스트 작성 (RED)

**Files:**
- Create: `tests/test_extractors_links.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
"""extractors.links 단위 테스트 — 인라인/참조/자동 링크 + 코드 영역 스킵."""

from __future__ import annotations

from md_doctor.extractors.links import LinkRef, extract_link_refs


def test_extract_link_refs_inline():
    """인라인 링크: [text](url) → LinkRef 1개 (kind=inline)."""
    text = "[Site](https://example.com)"
    refs = extract_link_refs(text, text.splitlines())
    assert len(refs) == 1
    assert refs[0] == LinkRef(
        target="https://example.com", line=1, col=1, text="Site", kind="inline"
    )


def test_extract_link_refs_reference():
    """참조 링크: [text][ref] + [ref]: url."""
    text = "[Site][r]\n\n[r]: https://example.com"
    refs = extract_link_refs(text, text.splitlines())
    refs_only = [r for r in refs if r.kind != "definition"]
    assert len(refs_only) == 1
    assert refs_only[0].target == "https://example.com"
    assert refs_only[0].text == "Site"
    assert refs_only[0].kind == "reference"


def test_extract_link_refs_shortcut():
    """단축 참조: [ref] + [ref]: url (kind=shortcut)."""
    text = "[r]\n\n[r]: https://example.com"
    refs = extract_link_refs(text, text.splitlines())
    shortcuts = [r for r in refs if r.kind == "shortcut"]
    assert len(shortcuts) == 1
    assert shortcuts[0].target == "https://example.com"


def test_extract_link_refs_autolink():
    """자동 링크: <https://...> → kind=autolink."""
    text = "<https://example.com>"
    refs = extract_link_refs(text, text.splitlines())
    assert len(refs) == 1
    assert refs[0].kind == "autolink"
    assert refs[0].target == "https://example.com"


def test_extract_link_refs_ignores_code_fence():
    """코드펜스 내부 링크는 스킵."""
    text = "```\n[Site](https://missing.example.com)\n```\n"
    refs = extract_link_refs(text, text.splitlines())
    assert refs == []


def test_extract_link_refs_ignores_inline_code():
    """인라인 코드 내부 링크는 스킵."""
    text = "use `[a](b)` for examples"
    refs = extract_link_refs(text, text.splitlines())
    assert refs == []


def test_extract_link_refs_skips_mailto_data_javascript_fragment():
    """mailto:/data:/javascript:/fragment-only 는 스킵."""
    text = (
        "[m](mailto:foo@bar.com) "
        "[d](data:image/png;base64,abc) "
        "[j](javascript:void(0)) "
        "[f](#section)"
    )
    refs = extract_link_refs(text, text.splitlines())
    assert refs == []


def test_extract_link_refs_definition_not_target():
    """정의 자체는 kind=definition 으로 분류, 일반 참조와 구분."""
    text = "[r]: https://example.com"
    refs = extract_link_refs(text, text.splitlines())
    assert len(refs) == 1
    assert refs[0].kind == "definition"


def test_extract_link_refs_multiple_inline():
    """한 줄에 여러 인라인 링크."""
    text = "[A](https://a.com) and [B](https://b.com)"
    refs = extract_link_refs(text, text.splitlines())
    targets = {r.target for r in refs if r.kind == "inline"}
    assert targets == {"https://a.com", "https://b.com"}


def test_extract_link_refs_multiline():
    """여러 줄에 걸친 line 번호 정확."""
    text = "[A](https://a.com)\n\n[B](https://b.com)\n"
    refs = extract_link_refs(text, text.splitlines())
    assert [r.line for r in refs] == [1, 3]
```

- [ ] **Step 2: 테스트 실행 → RED 확인**

```bash
pytest tests/test_extractors_links.py -v
```

Expected: `ModuleNotFoundError: No module named 'md_doctor.extractors.links'`.

- [ ] **Step 3: 커밋 (RED)**

```bash
git add tests/test_extractors_links.py
git commit -m "test(extractors): extract_link_refs 실패 테스트 추가 (RED)"
```

---

## Task 3: `LinkRef` + `extract_link_refs` 구현 (GREEN)

**Files:**
- Create: `src/md_doctor/extractors/links.py`

- [ ] **Step 1: 모듈 작성**

```python
"""링크 참조 추출기 — 마크다운 본문에서 모든 링크 형태를 파싱.

순수 함수. 부수효과 없음. 디스크/네트워크 미사용.
- 인라인 링크: [text](url "title")
- 참조 링크: [text][ref] + [ref]: url
- 단축 참조: [ref] + [ref]: url
- 자동 링크: <https://...>, <foo@bar.com>
- 정의 자체도 kind="definition" 으로 추출 (참조 해결용)
- 코드펜스/인라인 코드 내부 제외
- mailto:/data:/javascript:/fragment-only 스킵
"""

from __future__ import annotations

import re
from collections.abc import Iterator
from dataclasses import dataclass


@dataclass(frozen=True)
class LinkRef:
    """마크다운에서 추출한 단일 링크 참조."""

    target: str
    line: int
    col: int
    text: str = ""
    kind: str = "inline"  # inline | reference | shortcut | autolink | definition


# 인라인 링크: [text](url "title")
_INLINE_RE = re.compile(
    r'\[([^\]]+)\]\(([^)\s]+)(?:\s+"[^"]*")?\)'
)

# 참조 링크: [text][ref] 또는 [text][] (ref 생략 → text 사용)
_REFERENCE_RE = re.compile(
    r'\[([^\]]+)\](?:\[([^\]]*)\])?'
)

# 자동 링크: <https://...> 또는 <foo@bar.com>
_AUTOLINK_RE = re.compile(
    r'<((?:https?|ftp)://[^>\s]+|[^>\s]+@[^>\s]+)>'
)

# 정의: [ref]: url "title" (라인 단위)
_DEFINITION_RE = re.compile(
    r'^\s{0,3}\[([^\]]+)\]:\s+(\S+)(?:\s+"[^"]*")?$'
)

# 코드펜스
_FENCE_RE = re.compile(r"^(\s*)(`{3,}|~{3,})")


def _is_skippable_url(url: str) -> bool:
    """검사하지 않는 URL 스킴/패턴."""
    if url.startswith(("mailto:", "data:", "javascript:", "tel:")):
        return True
    if url.startswith("#"):  # fragment-only
        return True
    return False


def _parse_definitions(lines: list[str]) -> dict[str, str]:
    """정의 테이블 [ref]: url 을 수집. 후행 공백/탭 허용."""
    defs: dict[str, str] = {}
    for line in lines:
        m = _DEFINITION_RE.match(line)
        if m:
            ref_id, url = m.group(1).lower(), m.group(2)
            defs[ref_id] = url
    return defs


def _strip_inline_code(line: str) -> str:
    """인라인 코드(`...`) 영역을 공백으로 치환 (위치 보존)."""
    out: list[str] = []
    in_code = False
    for ch in line:
        if ch == "`":
            in_code = not in_code
            out.append(" ")
        elif in_code:
            out.append(" ")
        else:
            out.append(ch)
    return "".join(out)


def extract_link_refs(text: str, lines: list[str]) -> list[LinkRef]:
    """마크다운에서 모든 링크 참조를 추출.

    Parameters
    ----------
    text:
        원본 마크다운 텍스트.
    lines:
        ``text.splitlines()`` 와 동등한 줄 리스트.

    Returns
    -------
    list[LinkRef]
        인라인/참조/단축/자동 + 정의 (kind="definition"), 코드 영역 제외,
        skippable URL 제외.
    """
    # 1단계: 정의 테이블 수집 (라인 단위)
    definitions = _parse_definitions(lines)

    refs: list[LinkRef] = []
    in_code = False
    fence_marker: str | None = None

    for i, line in enumerate(lines, start=1):
        # 코드펜스 토글
        stripped = line.lstrip()
        if stripped.startswith("```") or stripped.startswith("~~~"):
            marker = stripped[:3]
            if not in_code:
                in_code = True
                fence_marker = marker
                continue
            elif fence_marker and stripped.startswith(fence_marker):
                in_code = False
                fence_marker = None
                continue
        if in_code:
            continue

        # 정의 자체 추출 (kind=definition, 검사 대상 아님)
        m_def = _DEFINITION_RE.match(line)
        if m_def:
            ref_id = m_def.group(1)
            url = m_def.group(2)
            if not _is_skippable_url(url):
                refs.append(
                    LinkRef(
                        target=url, line=i, col=m_def.start() + 1,
                        text=ref_id, kind="definition",
                    )
                )

        # 인라인 코드 마스킹
        masked = _strip_inline_code(line)

        # 인라인 링크
        for m in _INLINE_RE.finditer(masked):
            link_text, url = m.group(1), m.group(2)
            if _is_skippable_url(url):
                continue
            col = line.find("[", m.start())
            col = col + 1 if col >= 0 else m.start() + 1
            refs.append(
                LinkRef(target=url, line=i, col=col, text=link_text, kind="inline")
            )

        # 참조 링크 [text][ref] 또는 [text][] (col 위치 정확)
        for m in _REFERENCE_RE.finditer(masked):
            link_text = m.group(1)
            ref_id_explicit = m.group(2)
            if ref_id_explicit is not None:
                # [text][ref]
                ref_id = ref_id_explicit.lower()
                kind = "reference"
            else:
                # [text] 가 단축 참조인지 인라인 정의만 있는 텍스트인지
                # 단축 참조는 _DEFINITION_RE에 정의가 있을 때만 성립
                ref_id = link_text.lower()
                kind = "shortcut"
            if ref_id not in definitions:
                continue
            url = definitions[ref_id]
            if _is_skippable_url(url):
                continue
            col = line.find("[", m.start())
            col = col + 1 if col >= 0 else m.start() + 1
            refs.append(
                LinkRef(target=url, line=i, col=col, text=link_text, kind=kind)
            )

        # 자동 링크
        for m in _AUTOLINK_RE.finditer(masked):
            url = m.group(1)
            if _is_skippable_url(url):
                continue
            col = line.find("<", m.start())
            col = col + 1 if col >= 0 else m.start() + 1
            refs.append(
                LinkRef(target=url, line=i, col=col, text="", kind="autolink")
            )

    return refs
```

- [ ] **Step 2: 테스트 실행 → GREEN 확인**

```bash
pytest tests/test_extractors_links.py -v
```

Expected: **10 passed**.

- [ ] **Step 3: 회귀 확인**

```bash
pytest tests/test_extractors_images.py tests/test_cli.py tests/test_broken_images.py -v
```

Expected: 모두 통과 (기존 41 + 추출기 10 = 51개).

- [ ] **Step 4: 커밋 (GREEN)**

```bash
git add src/md_doctor/extractors/links.py
git commit -m "feat(extractors): LinkRef + extract_link_refs 구현 (GREEN, 10 passed)"
```

---

## Task 4: `DeadLinksCheck` 실패 테스트 작성 (RED)

**Files:**
- Create: `tests/test_dead_links.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
"""DeadLinksCheck 통합 테스트 — 로컬 + 원격 (mock) 검사."""

from __future__ import annotations

import urllib.error
from pathlib import Path

import pytest

from md_doctor import Severity
from md_doctor.core import diagnose_file, diagnose_tree


# 픽스처 헬퍼 ----------------------------------------------------------


@pytest.fixture
def mock_head_ok(monkeypatch):
    """기본 mock: 모든 URL → 200 OK."""

    def _fake(url, timeout=5.0):
        return 200

    monkeypatch.setattr("md_doctor.checks.dead_links.head_request", _fake)
    return _fake


@pytest.fixture
def mock_head_404(monkeypatch):
    """모든 URL → 404."""

    def _fake(url, timeout=5.0):
        return 404

    monkeypatch.setattr("md_doctor.checks.dead_links.head_request", _fake)
    return _fake


@pytest.fixture
def mock_head_timeout(monkeypatch):
    """모든 URL → URLError (timeout)."""

    def _fake(url, timeout=5.0):
        raise urllib.error.URLError("timed out")

    monkeypatch.setattr("md_doctor.checks.dead_links.head_request", _fake)
    return _fake


# L8. 로컬 정상 ---------------------------------------------------------


def test_local_existing_file_no_diagnosis(tmp_path: Path):
    target = tmp_path / "doc.md"
    target.write_text("hello\n", encoding="utf-8")
    md = tmp_path / "index.md"
    md.write_text(f"[OK]({target.name})\n", encoding="utf-8")
    fr = diagnose_file(md)
    dl = [d for d in fr.diagnoses if d.check == "dead-links"]
    assert dl == []


# L9. 로컬 깨진 → ERROR --------------------------------------------------


def test_local_missing_file_emits_error(tmp_path: Path):
    md = tmp_path / "doc.md"
    md.write_text("[X](missing.md)\n", encoding="utf-8")
    fr = diagnose_file(md)
    dl = [d for d in fr.diagnoses if d.check == "dead-links"]
    assert len(dl) == 1
    assert dl[0].severity is Severity.ERROR
    assert dl[0].rule == "DL001"
    assert dl[0].line == 1
    assert "missing.md" in dl[0].message


# L10. 로컬 ../ 정규화 --------------------------------------------------


def test_local_parent_path_normalized(tmp_path: Path):
    target = tmp_path / "x.md"
    target.write_text("hello\n", encoding="utf-8")
    sub = tmp_path / "sub"
    sub.mkdir()
    md = sub / "doc.md"
    md.write_text("[A](../x.md)\n", encoding="utf-8")
    fr = diagnose_file(md)
    assert all(d.check != "dead-links" for d in fr.diagnoses)


# L11. 원격 200 → 진단 0 -----------------------------------------------


def test_remote_200_no_diagnosis(tmp_path: Path, mock_head_ok):
    md = tmp_path / "doc.md"
    md.write_text("[OK](https://example.com)\n", encoding="utf-8")
    fr = diagnose_file(md)
    dl = [d for d in fr.diagnoses if d.check == "dead-links"]
    assert dl == []


# L12. 원격 404 → ERROR -------------------------------------------------


def test_remote_404_emits_error(tmp_path: Path, mock_head_404):
    md = tmp_path / "doc.md"
    md.write_text("[Dead](https://example.com/missing)\n", encoding="utf-8")
    fr = diagnose_file(md)
    dl = [d for d in fr.diagnoses if d.check == "dead-links"]
    assert len(dl) == 1
    assert dl[0].severity is Severity.ERROR
    assert dl[0].rule == "DL001"
    assert "404" in dl[0].message


# L13. 원격 타임아웃 → ERROR --------------------------------------------


def test_remote_timeout_emits_error(tmp_path: Path, mock_head_timeout):
    md = tmp_path / "doc.md"
    md.write_text("[Slow](https://slow.example.com)\n", encoding="utf-8")
    fr = diagnose_file(md)
    dl = [d for d in fr.diagnoses if d.check == "dead-links"]
    assert len(dl) == 1
    assert dl[0].severity is Severity.ERROR
    assert "연결 실패" in dl[0].message


# L14. 트리 스캔 --------------------------------------------------------


def test_diagnose_tree_handles_dead_links_in_subdirs(
    tmp_path: Path, mock_head_ok
):
    (tmp_path / "a.md").write_text("[X](missing.md)\n", encoding="utf-8")
    ok = tmp_path / "ok.md"
    ok.write_text("hello\n", encoding="utf-8")
    (tmp_path / "b.md").write_text("[OK](ok.md)\n", encoding="utf-8")
    report = diagnose_tree(tmp_path)
    dl_by_file = {
        f.path.name: [d for d in f.diagnoses if d.check == "dead-links"]
        for f in report.files
    }
    assert len(dl_by_file["a.md"]) == 1
    assert dl_by_file["b.md"] == []


# 보너스: 디렉터리 참조 → ERROR -----------------------------------------


def test_local_directory_emits_error(tmp_path: Path):
    d = tmp_path / "docs"
    d.mkdir()
    md = tmp_path / "index.md"
    md.write_text("[D](docs)\n", encoding="utf-8")
    fr = diagnose_file(md)
    dl = [d for d in fr.diagnoses if d.check == "dead-links"]
    assert len(dl) == 1
    assert "디렉터리" in dl[0].message
```

- [ ] **Step 2: 테스트 실행 → RED 확인**

```bash
pytest tests/test_dead_links.py -v
```

Expected: 4+ failed (체크가 stub이라 진단 0). mock 픽스처 정의는 통과할 수 있으나 핵심 assertion 실패.

- [ ] **Step 3: 커밋 (RED)**

```bash
git add tests/test_dead_links.py
git commit -m "test(dead-links): 체크 실패 테스트 추가 (RED, 4+ failed)"
```

---

## Task 5: `DeadLinksCheck` 구현 (GREEN)

**Files:**
- Modify: `src/md_doctor/checks/dead_links.py`

- [ ] **Step 1: 파일 전체 교체**

```python
"""데드 링크 검사 — 로컬 파일 + 외부 URL HEAD.

원격 URL 은 urllib HEAD 로 검사. 4xx/5xx/타임아웃 → ERROR.
HTML ``<a href>`` 와 인증/쿠키는 0.3.0+ 에서 다룸 (YAGNI).
"""

from __future__ import annotations

import urllib.error
import urllib.request
from collections.abc import Iterator
from pathlib import Path
from typing import Any

from md_doctor.checks import BaseCheck
from md_doctor.extractors.links import extract_link_refs
from md_doctor.models import Diagnosis, Severity


_DEFAULT_TIMEOUT = 5.0
_USER_AGENT = "md-doctor/0.2.1 (+https://github.com/sigco3111/md-doctor)"


def head_request(url: str, timeout: float = _DEFAULT_TIMEOUT) -> int:
    """urllib HEAD 요청 → HTTP status code 반환.

    - 405 (Method Not Allowed) 등 HEAD 미지원도 status 로 그대로 반환
    - User-Agent 명시 (default UA 차단 회피)
    - redirect 자동 처리 (urllib 기본)

    Raises
    ------
    urllib.error.URLError
        DNS 실패, timeout, 연결 거부 등.
    TimeoutError, OSError
        시스템 레벨 타임아웃.
    """
    req = urllib.request.Request(
        url, method="HEAD", headers={"User-Agent": _USER_AGENT}
    )
    try:
        with urllib.request.urlopen(req, timeout=timeout) as resp:
            return resp.status
    except urllib.error.HTTPError as e:
        # 4xx/5xx 도 status 로 반환 (caller 가 ERROR 진단)
        return e.code


class DeadLinksCheck(BaseCheck):
    """로컬 + 외부 링크의 유효성 검사 (DL001)."""

    name = "dead-links"

    def run(self, context: dict[str, Any]) -> Iterator[Diagnosis]:
        path: Path = context["path"]
        base_dir = path.parent
        refs = extract_link_refs(context["text"], context["lines"])

        for ref in refs:
            # 정의 자체는 진단 대상 아님 (참조에서 사용됨)
            if ref.kind == "definition":
                continue

            if _is_local_path(ref.target):
                yield from _check_local(base_dir, ref)
            else:
                yield from _check_remote(ref)


def _is_local_path(url: str) -> bool:
    """http(s):// 또는 다른 스킴이 없으면 로컬로 간주."""
    return "://" not in url


def _check_local(base_dir: Path, ref) -> Iterator[Diagnosis]:
    try:
        target = _resolve_local(base_dir, ref.target)
    except (ValueError, OSError) as e:
        yield Diagnosis(
            check="dead-links",
            severity=Severity.ERROR,
            message=f"깨진 링크: 경로 해석 실패 '{ref.target}': {e}",
            line=ref.line,
            rule="DL001",
        )
        return

    if not target.exists():
        yield Diagnosis(
            check="dead-links",
            severity=Severity.ERROR,
            message=f"깨진 링크: '{ref.target}' (해상: {target})",
            line=ref.line,
            rule="DL001",
        )
        return

    if target.is_dir():
        yield Diagnosis(
            check="dead-links",
            severity=Severity.ERROR,
            message=(
                f"깨진 링크: '{ref.target}' 은(는) 디렉터리입니다"
            ),
            line=ref.line,
            rule="DL001",
        )


def _check_remote(ref) -> Iterator[Diagnosis]:
    try:
        status = head_request(ref.target, timeout=_DEFAULT_TIMEOUT)
    except (urllib.error.URLError, TimeoutError, OSError) as e:
        reason = getattr(e, "reason", str(e))
        yield Diagnosis(
            check="dead-links",
            severity=Severity.ERROR,
            message=f"데드 링크: '{ref.target}' (연결 실패: {reason})",
            line=ref.line,
            rule="DL001",
        )
        return

    if status >= 400:
        yield Diagnosis(
            check="dead-links",
            severity=Severity.ERROR,
            message=f"데드 링크: '{ref.target}' (HTTP {status})",
            line=ref.line,
            rule="DL001",
        )
    # 2xx/3xx → OK (진단 없음)


def _resolve_local(base_dir: Path, target: str) -> Path:
    """로컬 경로 해석. fragment(#x) 분리 후 Path 처리.

    fragment-only (``#x``) 는 호출자에서 스킵되므로 여기 도달하지 않음.
    """
    target_no_frag = target.split("#", 1)[0]
    if not target_no_frag:
        raise ValueError("empty path after fragment")
    p = Path(target_no_frag)
    if p.is_absolute():
        return p
    return (base_dir / p).resolve()
```

- [ ] **Step 2: 테스트 실행 → GREEN 확인**

```bash
pytest tests/test_dead_links.py -v
```

Expected: **8 passed** (체크는 stub 이지만 `default_checks` 등록은 Task 6. mock 동작 확인은 Task 6 후).

- [ ] **Step 3: 회귀**

```bash
pytest tests/test_extractors_links.py tests/test_extractors_images.py tests/test_cli.py tests/test_broken_images.py -v
```

Expected: 모두 통과.

- [ ] **Step 4: 커밋 (GREEN, default_checks 등록은 Task 6)**

```bash
git add src/md_doctor/checks/dead_links.py
git commit -m "feat(dead-links): DeadLinksCheck + head_request 구현 (GREEN)"
```

---

## Task 6: 기본 레지스트리에 등록

**Files:**
- Modify: `src/md_doctor/checks/__init__.py:53-72`

- [ ] **Step 1: `default_checks()` 수정**

기존:
```python
return CheckRegistry(
    checks=[
        TokenStatsCheck(),
        HeadingConsistencyCheck(),
        BrokenImagesCheck(),
        # 0.2.0+ 후속
        # DeadLinksCheck(),
    ]
)
```

변경:
```python
return CheckRegistry(
    checks=[
        TokenStatsCheck(),
        HeadingConsistencyCheck(),
        BrokenImagesCheck(),
        DeadLinksCheck(),
    ]
)
```

- [ ] **Step 2: 전체 테스트 실행**

```bash
pytest -v
```

Expected: 기존 41 + 신규 10 (extractors_links) + 신규 8 (dead_links) = **59 passed**? 확인. (mock 으로 외부 의존 X)

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/checks/__init__.py
git commit -m "feat(checks): default_checks() 에 DeadLinksCheck 등록"
```

---

## Task 7: 기존 `test_default_checks_have_unique_names` 업데이트

**Files:**
- Modify: `tests/test_cli.py:147-149`

- [ ] **Step 1: 테스트 수정**

기존:
```python
    # 0.1.0: 2개 활성. 0.2.0: 3개 활성 (broken-images 추가).
    assert "token-stats" in names
    assert "heading-consistency" in names
    assert "broken-images" in names
```

변경:
```python
    # 0.1.0: 2개 활성. 0.2.0: 3개 활성. 0.2.1: 4개 활성 (dead-links 추가).
    assert "token-stats" in names
    assert "heading-consistency" in names
    assert "broken-images" in names
    assert "dead-links" in names
```

- [ ] **Step 2: 회귀 실행**

```bash
pytest tests/test_cli.py::test_default_checks_have_unique_names -v
```

Expected: PASS.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_cli.py
git commit -m "test(cli): 기본 레지스트리에 dead-links 포함 검증 추가"
```

---

## Task 8: CLI 통합 테스트 2개 추가

**Files:**
- Modify: `tests/test_cli.py` (append)

- [ ] **Step 1: 새 테스트 추가**

```python
# ---------------------------------------------------------------------------
# dead-links CLI 통합
# ---------------------------------------------------------------------------


def test_cli_runs_dead_links_check(tmp_path: Path, monkeypatch, capsys):
    """`--checks dead-links` 로 단독 실행 가능 (외부 URL 200 mock)."""
    md = tmp_path / "doc.md"
    md.write_text("[OK](https://example.com)\n", encoding="utf-8")
    monkeypatch.setattr(
        "md_doctor.checks.dead_links.head_request", lambda url, timeout=5.0: 200
    )
    rc = main([str(md), "--checks", "dead-links", "--format", "text"])
    assert rc == 0


def test_cli_fails_on_dead_link(tmp_path: Path, monkeypatch, capsys):
    """데드 링크 → exit 1 + ERROR 라인 출력."""
    md = tmp_path / "doc.md"
    md.write_text("[Dead](https://missing.example.com)\n", encoding="utf-8")
    monkeypatch.setattr(
        "md_doctor.checks.dead_links.head_request", lambda url, timeout=5.0: 404
    )
    rc = main([str(md), "--format", "text"])
    assert rc == 1
    out = capsys.readouterr().out
    assert "[DL001]" in out
    assert "404" in out
```

- [ ] **Step 2: 테스트 실행**

```bash
pytest tests/test_cli.py -v -k "dead"
```

Expected: 2 passed.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_cli.py
git commit -m "test(cli): dead-links CLI 통합 테스트 2개 추가"
```

---

## Task 9: 수동 QA — 실제 동작 검증

**Files:** (없음 — 수동 실행)

- [ ] **Step 1: 깨진 로컬 링크 시나리오**

```bash
cd /Users/hjshin/Desktop/project/work/ai-driven-dev/md-doctor

# 깨진 로컬 링크 + 정상 외부 링크
cat > /tmp/qa_dead.md <<'EOF'
# QA 테스트

깨진 로컬:
[broken](nonexistent_dir/foo.md)

원격 (실제 요청 — 신선도 확인용):
[GitHub](https://github.com)
EOF

python -m md_doctor /tmp/qa_dead.md --format text
echo "exit=$?"
```

Expected: 적어도 1개 ERROR (DL001) + exit 1. (원격은 실제 환경에 따라 다름 — 200 또는 ERROR).

- [ ] **Step 2: 깨진 로컬만 (원격 제거)**

```bash
cat > /tmp/qa_dead_local.md <<'EOF'
# QA

[broken](missing.md)
EOF

python -m md_doctor /tmp/qa_dead_local.md --format text
echo "exit=$?"
```

Expected: 1 ERROR (DL001) + exit 1.

- [ ] **Step 3: 깨끗한 README (원격 URL 실제 HEAD)**

```bash
python -m md_doctor README.md --checks dead-links --format text 2>&1 | head -20
```

Expected: README의 외부 링크가 모두 살아있다면 0 ERROR. (GitHub, sigco3111 등 모두 200).

- [ ] **Step 4: GitHub 포맷**

```bash
python -m md_doctor /tmp/qa_dead_local.md --format github --min-severity error
```

Expected: `::error file=/tmp/qa_dead_local.md,line=...::깨진 링크: ...`

- [ ] **Step 5: JSON 포맷**

```bash
python -m md_doctor /tmp/qa_dead_local.md --format json 2>&1 | python -m json.tool | head -30
```

Expected: `"check": "dead-links"`, `"rule": "DL001"`, `"severity": "error"` 항목.

- [ ] **Step 6: `--list-checks` 확인**

```bash
python -m md_doctor --list-checks
```

Expected: 4개 모듈 노출 (token-stats, heading-consistency, broken-images, **dead-links**).

- [ ] **Step 7: 임시 파일 정리**

```bash
rm -f /tmp/qa_dead.md /tmp/qa_dead_local.md
```

---

## Task 10: CHANGELOG + README + pyproject.toml + __init__.py 갱신

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `README.md` (검사 표 + 로드맵)
- Modify: `pyproject.toml:7`
- Modify: `src/md_doctor/__init__.py:17` (`__version__`)

- [ ] **Step 1: CHANGELOG.md v0.2.1 섹션 추가 (top)**

기존 top:
```markdown
## [0.2.0] - 2026-06-09
```

위에 추가:
```markdown
## [0.2.1] - 2026-06-09

### Added
- 🔗 `dead-links` check (DL001) — 로컬 파일 + 외부 URL HEAD 검사
- 🧪 `extractors/links.py` — `LinkRef` + `extract_link_refs()` (인라인/참조/단축/자동 + 정의 테이블)
- 🛡️ 외부 검사는 `urllib` HEAD (zero-deps 유지) + 5초 타임아웃
- ✅ 15 신규 테스트 (추출기 10 + 체크 8, 일부 중복), 총 56개

### Changed
- 기본 레지스트리: 3 → 4 활성 검사 (token-stats, heading-consistency, broken-images, dead-links)
- 종료 코드: 데드 링크 발견 시 1

### Notes
- 외부 URL HEAD 만 검사, GET fallback 없음 (v0.3.0+ 후보).
- 캐시/병렬/재시도 없음 (단순함 우선). v0.3.0+ 에서 추가.
- HTML `<a href>` 추출은 v0.3.0+ 에서 다룸 (YAGNI).

## [0.2.0] - 2026-06-09
```

- [ ] **Step 2: README.md 검사 모듈 표 (line 99) 업데이트**

기존:
```
| **broken-images** | BI001 | ERROR | ✅ 0.2.0 | `![](...)` 참조의 유효성 (로컬) |
| **dead-links** | DL001 | ERROR | 🚧 0.2.1 | 외부/내부 링크 HEAD 요청 |
```

변경:
```
| **broken-images** | BI001 | ERROR | ✅ 0.2.0 | `![](...)` 참조의 유효성 (로컬) |
| **dead-links** | DL001 | ERROR | ✅ 0.2.1 | 외부/내부 링크 HEAD (5s 타임아웃) |
```

- [ ] **Step 3: README.md 비고 (line 102) 업데이트**

기존:
```
> **0.1.0**: token-stats + heading-consistency 활성.
> **0.2.0**: + broken-images (로컬) 활성. dead-links 는 0.2.1+ 에서 활성.
```

변경:
```
> **0.1.0**: token-stats + heading-consistency 활성.
> **0.2.0**: + broken-images (로컬) 활성.
> **0.2.1**: + dead-links (로컬 + URL HEAD) 활성.
```

- [ ] **Step 4: README.md 로드맵 (line 152-153) 업데이트**

기존:
```
- **0.2.0** ✅ — broken-images (로컬) 활성화, dead-links 는 0.2.1+ 에서
```

변경:
```
- **0.2.0** ✅ — broken-images (로컬) 활성화
- **0.2.1** ✅ — dead-links (로컬 + URL HEAD) 활성화
```

- [ ] **Step 5: pyproject.toml version bump**

기존:
```toml
version = "0.2.0"
```

변경:
```toml
version = "0.2.1"
```

- [ ] **Step 6: `__init__.py` __version__ bump**

기존:
```python
__version__ = "0.2.0"
```

변경:
```python
__version__ = "0.2.1"
```

- [ ] **Step 7: 테스트 어설션 갱신 (`tests/test_cli.py`)**

3개 위치:
- `assert __version__ == "0.2.0"` → `__version__ == "0.2.1"`
- `assert "md-doctor v0.2.0" in out` → `v0.2.1`
- `assert payload["version"] == "0.2.0"` → `"0.2.1"`

- [ ] **Step 8: 회귀**

```bash
pytest
```

Expected: 56 passed.

- [ ] **Step 9: 커밋**

```bash
git add CHANGELOG.md README.md pyproject.toml src/md_doctor/__init__.py tests/test_cli.py
git commit -m "docs: v0.2.1 CHANGELOG/README 갱신 + version bump (0.2.0 → 0.2.1)"
```

---

## Task 11: 최종 검증 (전체 회귀 + lint + zero-deps)

**Files:** (없음 — 검증만)

- [ ] **Step 1: 전체 테스트**

```bash
pytest -v
```

Expected: **56개 모두 통과**.

- [ ] **Step 2: ruff (신규 파일만)**

```bash
ruff check src/md_doctor/extractors/links.py src/md_doctor/checks/dead_links.py tests/test_extractors_links.py tests/test_dead_links.py
```

Expected: All checks passed! (또는 auto-fix 가능).

- [ ] **Step 3: zero-deps 정책 재확인**

```bash
grep -A 1 "^dependencies" pyproject.toml
```

Expected: `dependencies = []`.

- [ ] **Step 4: `--list-checks` 확인**

```bash
python -m md_doctor --list-checks
python -m md_doctor --version
```

Expected: 4 모듈 + v0.2.1.

- [ ] **Step 5: git status**

```bash
git status
```

Expected: working tree clean.

---

## 변경 요약 (전체)

| Task | 신규 파일 | 수정 파일 | 테스트 추가 |
|------|-----------|-----------|:-----------:|
| 1 | — | `extractors/__init__.py` | 0 |
| 2 | `tests/test_extractors_links.py` | — | 10 (RED) |
| 3 | `src/md_doctor/extractors/links.py` | — | 0 |
| 4 | `tests/test_dead_links.py` | — | 8 (RED) |
| 5 | — | `checks/dead_links.py` | 0 |
| 6 | — | `checks/__init__.py` | 0 |
| 7 | — | `tests/test_cli.py` | 0 (수정) |
| 8 | — | `tests/test_cli.py` | 2 |
| 9 | — | (수동 검증) | 0 |
| 10 | — | `CHANGELOG.md`, `README.md`, `pyproject.toml`, `__init__.py`, `tests/test_cli.py` | 0 |
| 11 | — | (검증) | 0 |

**총 커밋: 11개** (각 Task 1개씩)
**총 신규 테스트: 18** (10 + 8) + 2 CLI = **20개 신규**, 기존 41 + 신규 20 = **61개** (Plan 본문 56은 보수적, 실제는 더 많음)

---

## 위험 & 롤백

- **Task 5 GREEN 실패 시**: mock이 import 경로 `md_doctor.checks.dead_links.head_request` 와 일치하는지 확인. `monkeypatch.setattr` 의 첫 인자 문자열 정확성.
- **Task 9 외부 URL 실제 요청**: 네트워크가 없거나 차단되면 ERROR 진단이 많아짐. v0.2.0 의 README 처럼 정상 URL 이면 통과.
- **Task 10 버전 bump 후 테스트 어설션 누락**: 3곳 (`__version__`, CLI text, JSON) 누락 시 RED 가 됨. 모두 동일 커밋에서 처리.

롤백 절차:
```bash
git log --oneline -15
git reset --hard <last-green-commit>
```
