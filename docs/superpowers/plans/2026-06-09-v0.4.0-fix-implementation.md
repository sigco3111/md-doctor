# v0.4.0 md-doctor fix — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `md-doctor fix` subcommand 를 추가하여 6개 안전한 자동 변환(trailing newline, trailing whitespace, heading-newline, empty heading, BOM, CRLF) 을 in-place 또는 dry-run 으로 적용한다. 기존 `md-doctor <path>` (diagnose) 동작은 100% 보존한다.

**Architecture:** `fixers/` 패키지 신규 분리 (단일 책임, 진단과 독립). `fixers/transforms.py` 가 6개 변환 함수 + `apply_fixes()` 체인. `cli.py` 가 argparse subcommand 로 `fix` 를 추가 (기존 `diagnose` 는 default). `--backup .bak` 기본, `--dry-run`/`--no-backup`/`--format json` 옵션.

**Tech Stack:** Python 3.9+ stdlib only (zero-deps), pytest, argparse, shutil, json, re. 외부 라이브러리 추가 없음.

**Spec:** `docs/superpowers/specs/2026-06-09-v0.4.0-fix-design.md`

**저장소:** `/Users/hjshin/Desktop/project/work/ai-driven-dev/md-doctor`

---

## 파일 구조 (변경 요약)

| 작업 | 파일 | 책임 |
|------|------|------|
| Create | `src/md_doctor/fixers/__init__.py` | re-export `apply_fixes`, `FixResult` |
| Create | `src/md_doctor/fixers/transforms.py` | 6개 변환 + `apply_fixes()` |
| Modify | `src/md_doctor/cli.py` | argparse subcommand `fix` 추가, dispatch 로직 |
| Modify | `pyproject.toml` | version `0.3.0` → `0.4.0` |
| Modify | `CHANGELOG.md` | v0.4.0 섹션 추가 |
| Modify | `README.md` | `md-doctor fix` 사용 예 + 로드맵 |
| Modify | `src/md_doctor/__init__.py` | `__version__` |
| Modify | `tests/test_cli.py` | 버전 어설션 (3개) |
| Create | `tests/test_fixers.py` | 10 케이스 |
| Create | `tests/test_fix.py` | CLI 5 케이스 |

총 변경: 신규 3, 수정 5. 기존 92 + 신규 15 = **107개** 목표.

---

## Task 1: `fixers/transforms.py` RED 테스트

**Files:**
- Create: `tests/test_fixers.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
"""fixers.transforms 단위 테스트."""

from __future__ import annotations

from md_doctor.fixers import FixResult, apply_fixes


def test_fix_f1_trailing_newline():
    """F1: 파일 끝 newline 1개 보장."""
    result = apply_fixes("text without newline")
    assert result.fixed == "text without newline\n"
    assert "F1" in result.changes


def test_fix_f2_trailing_whitespace():
    """F2: 라인 끝 공백 제거."""
    result = apply_fixes("line 1   \nline 2\t\n")
    assert result.fixed == "line 1\nline 2\n"
    assert "F2" in result.changes


def test_fix_f3_heading_blank_line():
    """F3: H1/H2 헤딩 다음 빈 줄 보장."""
    result = apply_fixes("# Title\n본문")
    assert result.fixed == "# Title\n\n본문"
    assert "F3" in result.changes


def test_fix_f4_empty_heading():
    """F4: 빈 헤딩 제거."""
    result = apply_fixes("# Title\n## \n본문")
    assert "## " not in result.fixed
    assert "F4" in result.changes


def test_fix_f5_strip_bom():
    """F5: UTF-8 BOM 제거."""
    result = apply_fixes("\ufeff# Title")
    assert result.fixed == "# Title"
    assert "F5" in result.changes


def test_fix_f6_crlf_to_lf():
    """F6: CRLF → LF."""
    result = apply_fixes("line 1\r\nline 2\r\n")
    assert result.fixed == "line 1\nline 2\n"
    assert "F6" in result.changes


def test_fix_clean_no_changes():
    """깨끗한 파일 → 변경 0건."""
    text = "# Title\n\n본문\n"
    result = apply_fixes(text)
    assert result.changes == []
    assert result.fixed == text


def test_fix_empty_input():
    """빈 입력 → 변경 0건, 변경 없음."""
    result = apply_fixes("")
    assert result.fixed == ""
    assert result.changes == []


def test_fix_no_heading_no_f3():
    """헤딩 없으면 F3 스킵."""
    result = apply_fixes("본문만\n")
    assert "F3" not in result.changes


def test_fix_combined():
    """복합 변환 (BOM + trailing ws + CRLF)."""
    text = "\ufeff# Title  \r\n본문"
    result = apply_fixes(text)
    assert "F2" in result.changes
    assert "F5" in result.changes
    assert "F6" in result.changes
```

- [ ] **Step 2: RED 확인**

```bash
pytest tests/test_fixers.py -v
```

Expected: `ModuleNotFoundError: No module named 'md_doctor.fixers'`.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_fixers.py
git commit -m "test(fixers): 6 변환 실패 테스트 추가 (RED, 10 케이스)"
```

---

## Task 2: `fixers/transforms.py` 구현 (GREEN)

**Files:**
- Create: `src/md_doctor/fixers/transforms.py`

- [ ] **Step 1: 모듈 작성**

```python
"""자동 후처리 변환 — 6개 안전한 변형.

F1~F6 순서로 적용 (BOM 우선, CRLF 마지막).
"""

from __future__ import annotations

import re
from collections.abc import Callable
from dataclasses import dataclass, field


@dataclass(frozen=True)
class FixResult:
    """fix 1회의 결과."""

    original: str
    fixed: str
    changes: list[str] = field(default_factory=list)
    changed_lines: list[int] = field(default_factory=list)


def f1_trailing_newline(text: str) -> tuple[str, bool]:
    """F1: 파일 끝 newline 1개 보장."""
    if not text or text.endswith("\n"):
        return text, False
    return text + "\n", True


def f2_trailing_whitespace(text: str) -> tuple[str, bool]:
    """F2: 라인 끝 공백 제거."""
    lines = text.split("\n")
    new_lines = [line.rstrip() for line in lines]
    new_text = "\n".join(new_lines)
    return new_text, new_text != text


def f3_heading_blank_line(text: str) -> tuple[str, bool]:
    """F3: H1/H2 헤딩 다음 빈 줄 보장."""
    lines = text.split("\n")
    new_lines: list[str] = []
    heading_re = re.compile(r"^#{1,2}\s+\S")
    changed = False
    for i, line in enumerate(lines):
        new_lines.append(line)
        if (
            heading_re.match(line)
            and i + 1 < len(lines)
            and lines[i + 1].strip() != ""
        ):
            new_lines.append("")
            changed = True
    new_text = "\n".join(new_lines)
    return new_text, changed


def f4_empty_heading(text: str) -> tuple[str, bool]:
    """F4: 빈 헤딩 제거."""
    lines = text.split("\n")
    empty_re = re.compile(r"^#{1,6}\s*$")
    new_lines = [line for line in lines if not empty_re.match(line)]
    new_text = "\n".join(new_lines)
    return new_text, new_text != text


def f5_strip_bom(text: str) -> tuple[str, bool]:
    """F5: UTF-8 BOM 제거."""
    if text.startswith("\ufeff"):
        return text[1:], True
    return text, False


def f6_crlf_to_lf(text: str) -> tuple[str, bool]:
    """F6: CRLF → LF."""
    if "\r\n" in text:
        return text.replace("\r\n", "\n"), True
    return text, False


ALL_TRANSFORMS: list[tuple[str, Callable[[str], tuple[str, bool]]]] = [
    ("F5", f5_strip_bom),
    ("F1", f1_trailing_newline),
    ("F2", f2_trailing_whitespace),
    ("F3", f3_heading_blank_line),
    ("F4", f4_empty_heading),
    ("F6", f6_crlf_to_lf),
]


def _track_changed_lines(
    before: str, after: str, changed: set[int]
) -> None:
    """변경된 라인 추적 (간이: splitlines 비교)."""
    before_lines = before.split("\n")
    after_lines = after.split("\n")
    n = max(len(before_lines), len(after_lines))
    for i in range(n):
        b = before_lines[i] if i < len(before_lines) else None
        a = after_lines[i] if i < len(after_lines) else None
        if b != a:
            changed.add(i + 1)  # 1-based


def apply_fixes(
    text: str,
    *,
    enabled: set[str] | None = None,
) -> FixResult:
    """모든 enabled 변환을 순서대로 적용.

    enabled: None = 모두 적용. set 으로 활성화 subset 지정 가능.
    """
    enabled_ids = enabled or {fid for fid, _ in ALL_TRANSFORMS}
    current = text
    applied: list[str] = []
    changed_lines: set[int] = set()

    for fid, transform in ALL_TRANSFORMS:
        if fid not in enabled_ids:
            continue
        new_text, changed = transform(current)
        if changed:
            _track_changed_lines(current, new_text, changed_lines)
            applied.append(fid)
        current = new_text

    return FixResult(
        original=text,
        fixed=current,
        changes=applied,
        changed_lines=sorted(changed_lines),
    )
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_fixers.py -v
```

Expected: **10 passed**.

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/fixers/transforms.py
git commit -m "feat(fixers): 6 변환 + apply_fixes 구현 (GREEN, 10 passed)"
```

---

## Task 3: `fixers/__init__.py` re-export

**Files:**
- Create: `src/md_doctor/fixers/__init__.py`

- [ ] **Step 1: 작성**

```python
"""자동 후처리 모음 — 6개 안전한 변형.

진단과 독립적으로 동작. 사용자가 명시적으로 ``md-doctor fix`` 호출.
"""

from md_doctor.fixers.transforms import (
    ALL_TRANSFORMS,
    FixResult,
    apply_fixes,
    f1_trailing_newline,
    f2_trailing_whitespace,
    f3_heading_blank_line,
    f4_empty_heading,
    f5_strip_bom,
    f6_crlf_to_lf,
)

__all__ = [
    "ALL_TRANSFORMS",
    "FixResult",
    "apply_fixes",
    "f1_trailing_newline",
    "f2_trailing_whitespace",
    "f3_heading_blank_line",
    "f4_empty_heading",
    "f5_strip_bom",
    "f6_crlf_to_lf",
]
```

- [ ] **Step 2: 커밋**

```bash
git add src/md_doctor/fixers/__init__.py
git commit -m "feat(fixers): package re-export 추가"
```

---

## Task 4: CLI `fix` subcommand 추가

**Files:**
- Modify: `src/md_doctor/cli.py`

- [ ] **Step 1: argparse subcommand 추가 + dispatch**

기존 `_build_parser()` 와 `main()` 수정:

```python
import shutil
from md_doctor.fixers import FixResult, apply_fixes


def _build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(
        prog="md-doctor",
        description="마크다운 품질 진단 + 후처리 CLI",
    )
    subparsers = parser.add_subparsers(dest="command")
    
    # 기본 diagnose (command 미지정 시 default)
    diag = subparsers.add_parser("diagnose", help="진단 (기본)")
    diag.add_argument("path", type=Path, nargs="?", default=None)
    # ... 기존 인자들 ...
    
    # 신규 fix
    fix_p = subparsers.add_parser("fix", help="자동 후처리")
    fix_p.add_argument("path", type=Path)
    fix_p.add_argument("--backup", default=".bak")
    fix_p.add_argument("--no-backup", action="store_true")
    fix_p.add_argument("--dry-run", action="store_true")
    fix_p.add_argument(
        "--format", choices=["text", "json"], default="text"
    )
    fix_p.add_argument("-o", "--output", type=Path, default=None)
    
    return parser
```

**호환성 처리**: `args.command` 가 None 이면 `diagnose` 로 기본값.

```python
def main(argv: list[str] | None = None) -> int:
    parser = _build_parser()
    args = parser.parse_args(argv)
    
    # default command: 기존 diagnose
    if args.command is None:
        args.command = "diagnose"
    
    if args.command == "fix":
        return _run_fix(args)
    else:
        return _run_diagnose(args)  # 기존 main 로직
```

- [ ] **Step 2: `_run_fix()` 함수 추가**

```python
def _run_fix(args) -> int:
    from md_doctor import __version__
    path: Path = args.path
    if not path.exists():
        print(f"❌ 경로가 존재하지 않습니다: {path}", file=sys.stderr)
        return 2
    
    try:
        original = path.read_text(encoding="utf-8")
    except UnicodeDecodeError:
        original = path.read_text(encoding="utf-8", errors="replace")
    
    result = apply_fixes(original)
    
    if not args.dry_run and result.fixed != original:
        if not args.no_backup:
            backup_path = path.with_suffix(path.suffix + args.backup)
            shutil.copy2(path, backup_path)
        path.write_text(result.fixed, encoding="utf-8")
    
    if args.format == "json":
        from md_doctor import __version__
        payload = {
            "version": __version__,
            "file": str(path),
            "dry_run": args.dry_run,
            "applied": result.changes,
            "changed_lines": result.changed_lines,
            "before": result.original,
            "after": result.fixed,
        }
        out = json.dumps(payload, ensure_ascii=False, indent=2)
    else:
        lines: list[str] = []
        from md_doctor import __version__
        lines.append(f"🔧 md-doctor v{__version__} fix — {path}")
        if result.changes:
            lines.append(f"   ✅ 변경 {len(result.changes)}건 ({', '.join(result.changes)})")
            for ln in result.changed_lines:
                lines.append(f"   - 라인 {ln}")
        else:
            lines.append("   ✅ 변경 없음 (이미 깔끔)")
        if args.dry_run:
            lines.append("   (dry-run: 실제 변경 X)")
        out = "\n".join(lines)
    
    if args.output:
        args.output.write_text(out, encoding="utf-8")
    else:
        print(out)
    
    return 0
```

기존 `main()` 본문을 `_run_diagnose(args)` 함수로 분리.

- [ ] **Step 3: 회귀 (기존 diagnose 동작 보존)**

```bash
pytest tests/test_cli.py -v 2>&1 | tail -10
```

Expected: 기존 23개 모두 통과 (변경 없음).

- [ ] **Step 4: 커밋**

```bash
git add src/md_doctor/cli.py
git commit -m "feat(cli): 'fix' subcommand 추가 (in-place, --backup, --dry-run, --format json)"
```

---

## Task 5: CLI `fix` RED 테스트

**Files:**
- Create: `tests/test_fix.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
"""md-doctor fix CLI 통합 테스트."""

from __future__ import annotations

import json
from pathlib import Path

from md_doctor.cli import main


def test_cli_fix_creates_backup(tmp_path: Path, capsys):
    """기본: 백업 생성 + in-place."""
    md = tmp_path / "test.md"
    md.write_text("no newline")
    rc = main(["fix", str(md)])
    assert rc == 0
    assert (tmp_path / "test.md.bak").exists()
    assert md.read_text(encoding="utf-8") == "no newline\n"


def test_cli_fix_no_backup(tmp_path: Path):
    """--no-backup: 백업 생략."""
    md = tmp_path / "test.md"
    md.write_text("no newline")
    rc = main(["fix", str(md), "--no-backup"])
    assert rc == 0
    assert not (tmp_path / "test.md.bak").exists()
    assert md.read_text(encoding="utf-8") == "no newline\n"


def test_cli_fix_dry_run(tmp_path: Path):
    """--dry-run: 원본 보존."""
    md = tmp_path / "test.md"
    original = "no newline"
    md.write_text(original)
    rc = main(["fix", str(md), "--dry-run"])
    assert rc == 0
    assert md.read_text(encoding="utf-8") == original


def test_cli_fix_json_output(tmp_path: Path, capsys):
    """--format json: payload 출력."""
    md = tmp_path / "test.md"
    md.write_text("no newline")
    rc = main(["fix", str(md), "--no-backup", "--format", "json"])
    assert rc == 0
    payload = json.loads(capsys.readouterr().out)
    assert "F1" in payload["applied"]


def test_cli_diagnose_still_works(tmp_path: Path, capsys):
    """기존 md-doctor <path> (diagnose) 호환성."""
    md = tmp_path / "test.md"
    md.write_text("# Title\n\n- a\n- b\n", encoding="utf-8")
    rc = main([str(md), "--format", "text"])
    assert rc == 0
    out = capsys.readouterr().out
    assert "md-doctor" in out
```

- [ ] **Step 2: RED 확인**

```bash
pytest tests/test_fix.py -v
```

Expected: 4 failed (fix subcommand 없음), 1 passed (기존 diagnose).

- [ ] **Step 3: 커밋**

```bash
git add tests/test_fix.py
git commit -m "test(fix): fix subcommand CLI 통합 테스트 추가 (RED)"
```

---

## Task 6: GREEN 확인

```bash
pytest tests/test_fix.py -v 2>&1 | tail -10
```

Expected: **5 passed** (Task 4 의 `_run_fix` 구현이 이미 GREEN 만듦).

- [ ] **Step 1: 회귀 (전체)**

```bash
pytest 2>&1 | tail -3
```

Expected: 92 + 10 + 5 = **107 passed**.

- [ ] **Step 2: 커밋 (없으면 생략)**

```bash
git status
```

---

## Task 7: 수동 QA

- [ ] **Step 1: 깨끗한 파일**

```bash
cd /Users/hjshin/Desktop/project/work/ai-driven-dev/md-doctor

cat > /tmp/qa_fix_clean.md <<'EOF'
# Clean

본문만.
EOF

python -m md_doctor fix /tmp/qa_fix_clean.md --no-backup --dry-run
```

Expected: "변경 없음 (이미 깔끔)".

- [ ] **Step 2: 복합 위반**

```bash
printf '\xef\xbb\xbf# Title  \r\n본문\r\n## \r\n' > /tmp/qa_fix_bad.md
xxd /tmp/qa_fix_bad.md | head -2
python -m md_doctor fix /tmp/qa_fix_bad.md --no-backup --dry-run
```

Expected: F1, F2, F4, F5, F6 (F3 는 헤딩 후 빈 줄 검사이지만 # Title 뒤에 빈 줄 있음).

- [ ] **Step 3: in-place 적용 (실제 변경)**

```bash
python -m md_doctor fix /tmp/qa_fix_bad.md --no-backup
cat /tmp/qa_fix_bad.md
```

Expected: BOM 제거, trailing ws 제거, CRLF → LF, 빈 헤딩 제거, trailing newline 추가.

- [ ] **Step 4: 백업 생성**

```bash
printf "no newline" > /tmp/qa_fix_bak.md
python -m md_doctor fix /tmp/qa_fix_bak.md
ls /tmp/qa_fix_bak.md*
cat /tmp/qa_fix_bak.md.bak  # 원본 보존
```

Expected: `.bak` 파일에 원본 보존.

- [ ] **Step 5: JSON 출력**

```bash
printf "no newline" > /tmp/qa_fix_json.md
python -m md_doctor fix /tmp/qa_fix_json.md --no-backup --format json
```

Expected: JSON with `applied: ["F1"]`.

- [ ] **Step 6: 기존 diagnose 호환성**

```bash
python -m md_doctor README.md --format text 2>&1 | head -3
```

Expected: 기존과 동일하게 출력.

- [ ] **Step 7: 정리**

```bash
rm -f /tmp/qa_fix_*.md*
```

---

## Task 8: CHANGELOG + README + version

- [ ] **Step 1: CHANGELOG.md v0.4.0 섹션**

```markdown
## [0.4.0] - 2026-06-09

### Added
- 🔧 `md-doctor fix` subcommand — 6개 안전한 자동 변환
  - F1: 트레일링 newline 추가
  - F2: 트레일링 공백 제거
  - F3: H1/H2 헤딩 다음 빈 줄 보장
  - F4: 빈 헤딩 제거
  - F5: UTF-8 BOM 제거
  - F6: CRLF → LF
- 🧪 `fixers/` 패키지 — `apply_fixes()`, `FixResult`
- 🛡️ 기본 in-place + `--backup .bak`, `--no-backup`/`--dry-run`/`--format json` 옵션
- ✅ 15 신규 테스트 (fixers 10 + fix CLI 5), 총 107개

### Notes
- 진단과 독립 — 사용자가 명시적으로 `fix` 호출.
- 트리 fix 는 v0.4.0 범위 외 (v0.4.1+).
- zero-deps 정책 유지.

## [0.3.0] - 2026-06-09
```

- [ ] **Step 2: README 검사 모듈 표 유지 + `fix` 사용 예 추가**

기존 빠른 시작 섹션에:
```bash
# 자동 후처리 (안전한 변환만)
md-doctor fix README.md              # in-place + .bak 백업
md-doctor fix README.md --dry-run   # 변경 사항만 표시
md-doctor fix README.md --no-backup # 백업 생략
md-doctor fix README.md --format json -o diff.json
```

- [ ] **Step 3: README 로드맵 0.4.0 → ✅**

```
- **0.3.0** ✅ — GFM/CommonMark 린트 (gfm-lint + HTML 추출)
- **0.4.0** ✅ — md-doctor fix (6개 안전 변환)
```

- [ ] **Step 4: pyproject.toml + __init__.py + test_cli.py 버전 bump**

- [ ] **Step 5: 커밋**

```bash
pytest 2>&1 | tail -3
git add CHANGELOG.md README.md pyproject.toml src/md_doctor/__init__.py tests/test_cli.py
git commit -m "docs: v0.4.0 CHANGELOG/README 갱신 + version bump (0.3.0 → 0.4.0)"
```

---

## Task 9: 최종 검증

- [ ] **Step 1: 전체 테스트**

```bash
pytest -v 2>&1 | tail -10
```

Expected: **107 passed**.

- [ ] **Step 2: ruff 신규 파일**

```bash
ruff check src/md_doctor/fixers/ tests/test_fixers.py tests/test_fix.py
```

- [ ] **Step 3: zero-deps**

```bash
grep -A 1 "^dependencies" pyproject.toml
```

- [ ] **Step 4: CLI**

```bash
python -m md_doctor --version
python -m md_doctor fix --help
```

- [ ] **Step 5: git status**

```bash
git status
```

---

## 변경 요약 (전체)

| Task | 신규 | 수정 | 테스트 추가 |
|------|------|------|------------|
| 1-2 | 1 (test) + 1 (impl) | - | +10 |
| 3 | 1 (re-export) | - | 0 |
| 4 | - | 1 (cli) | 0 |
| 5-6 | 1 (test) | - | +5 |
| 7 | - | (수동) | 0 |
| 8 | - | 5 (docs+version+test) | 0 |
| 9 | - | (검증) | 0 |

**총 커밋**: 9
**최종 테스트**: 107 passed (기존 92 + 신규 15)

---

## 위험 & 롤백

- **Task 4 CLI 호환성**: `md-doctor <path>` 가 여전히 동작해야 함. subcommand 미지정 시 `diagnose` 로 default — Task 4 Step 3 회귀 23개 통과로 확인.
- **Task 4 파일 경로**: 디렉터리 입력 시 `path.read_text()` 가 `IsADirectoryError` → stderr 메시지 + exit 2.
- **Task 6 in-place**: dry-run 으로 먼저 확인 권장. README 자체에 fix 적용 시 README 자체 손상 가능 (테스트로 회귀 확인).

롤백:
```bash
git log --oneline -10
git reset --hard <last-green-commit>
```
