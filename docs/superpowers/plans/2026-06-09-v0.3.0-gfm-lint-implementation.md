# v0.3.0 gfm-lint — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GFM/CommonMark 규칙 린트(`gfm-lint` 단일 체크 — HC005~008) 와 이미지/링크 HTML 추출(BI002/DL002) 을 추가하여 v0.2.x 의 마크다운 검사 범위를 확장한다. zero-deps 정책과 v0.2.x 의 모든 기존 동작을 유지한다.

**Architecture:** v0.2.x 의 단일 책임 분리 패턴을 그대로 따름. **공통 헬퍼**(`extractors/code_fence.py` — 코드 영역 마킹) 를 도입하여 images/links/html 3개 추출기의 중복 로직을 통합. **HTML 추출기**(`extractors/html.py`) 를 신규로 만들어 `<img src>`, `<a href>` 등 6+ 태그를 추출. **gfm-lint 체크**(`checks/gfm_lint.py`) 가 4개 규칙(HC005-008)을 단일 체크로 발행. 기존 `BrokenImagesCheck`/`DeadLinksCheck` 가 HTML 추출을 추가로 활용(BI002/DL002).

**Tech Stack:** Python 3.9+ stdlib only (zero-deps), pytest, re, pathlib. 외부 라이브러리 추가 없음.

**Spec:** `docs/superpowers/specs/2026-06-09-v0.3.0-gfm-lint-design.md`
**v0.2.0 패턴:** `docs/superpowers/plans/2026-06-09-v0.2.0-broken-images-implementation.md`

**저장소:** `/Users/hjshin/Desktop/project/work/ai-driven-dev/md-doctor`

---

## 파일 구조 (변경 요약)

| 작업 | 파일 | 책임 |
|------|------|------|
| Create | `src/md_doctor/extractors/code_fence.py` | `CodeFence`, `mark_code_regions`, `is_in_code_region`, `strip_inline_code` |
| Create | `src/md_doctor/extractors/html.py` | `HtmlRef`, `extract_html_refs` |
| Create | `src/md_doctor/checks/gfm_lint.py` | `GfmLintCheck` (HC005~008) |
| Modify | `src/md_doctor/extractors/__init__.py` | 신규 re-export |
| Modify | `src/md_doctor/extractors/images.py` | `_strip_inline_code` 위임 |
| Modify | `src/md_doctor/extractors/links.py` | `_strip_inline_code`, 코드펜스 위임 |
| Modify | `src/md_doctor/checks/broken_images.py` | HTML 추출 → BI002 |
| Modify | `src/md_doctor/checks/dead_links.py` | HTML 추출 → DL002 |
| Modify | `src/md_doctor/checks/__init__.py` | `default_checks()` 에 `GfmLintCheck()` 추가 |
| Modify | `pyproject.toml` | version `0.2.1` → `0.3.0` |
| Modify | `CHANGELOG.md` | v0.3.0 섹션 추가 |
| Modify | `README.md` | 검사 모듈 표 + 로드맵 |
| Modify | `src/md_doctor/__init__.py` | `__version__` |
| Modify | `tests/test_cli.py` | 버전 어설션 + 기본 검사 검증 |
| Create | `tests/test_extractors_code_fence.py` | 6 케이스 |
| Create | `tests/test_extractors_html.py` | 8 케이스 |
| Create | `tests/test_gfm_lint.py` | 8 케이스 |
| Modify | `tests/test_broken_images.py` | +2 케이스 |
| Modify | `tests/test_dead_links.py` | +2 케이스 |

총 변경: 신규 6, 수정 11. 기존 61 + 신규 28 = **89개** 목표.

---

## Task 1: `extractors/code_fence.py` 실패 테스트 (RED)

**Files:**
- Create: `tests/test_extractors_code_fence.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
"""extractors.code_fence 단위 테스트."""

from __future__ import annotations

from md_doctor.extractors.code_fence import (
    CodeFence,
    is_in_code_region,
    mark_code_regions,
    strip_inline_code,
)


def test_mark_code_regions_no_fence():
    """코드펜스 없음 → 빈 리스트."""
    lines = ["hello", "world"]
    fences = mark_code_regions(lines)
    assert fences == []


def test_mark_code_regions_simple_fence():
    """단순 펜스 1개."""
    lines = ["```python", "print()", "```"]
    fences = mark_code_regions(lines)
    assert len(fences) == 1
    assert fences[0] == CodeFence(start_line=1, end_line=3, language="python", marker="```")


def test_mark_code_regions_tilde_fence():
    """~~~ 펜스."""
    lines = ["~~~bash", "echo hi", "~~~"]
    fences = mark_code_regions(lines)
    assert len(fences) == 1
    assert fences[0].marker == "~~~"


def test_mark_code_regions_unclosed():
    """미닫힌 펜스 → end_line=None."""
    lines = ["```", "code", "more"]
    fences = mark_code_regions(lines)
    assert len(fences) == 1
    assert fences[0].end_line is None


def test_is_in_code_region():
    """특정 줄이 코드 영역 내부인지."""
    lines = ["a", "```", "b", "```", "c"]
    fences = mark_code_regions(lines)
    assert is_in_code_region(1, fences) is False
    assert is_in_code_region(2, fences) is False  # fence 시작 자체
    assert is_in_code_region(3, fences) is True
    assert is_in_code_region(4, fences) is False  # fence 끝 자체
    assert is_in_code_region(5, fences) is False


def test_strip_inline_code_preserves_position():
    """인라인 코드 영역을 공백으로 치환, 위치 보존."""
    line = "a `code` b"
    result = strip_inline_code(line)
    assert result == "a       b"
    assert len(result) == len(line)
```

- [ ] **Step 2: RED 확인**

```bash
pytest tests/test_extractors_code_fence.py -v
```

Expected: `ModuleNotFoundError: No module named 'md_doctor.extractors.code_fence'`.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_extractors_code_fence.py
git commit -m "test(extractors): code_fence 실패 테스트 추가 (RED)"
```

---

## Task 2: `code_fence.py` 구현 (GREEN)

**Files:**
- Create: `src/md_doctor/extractors/code_fence.py`

- [ ] **Step 1: 모듈 작성**

```python
"""코드펜스 + 인라인 코드 마킹 — 추출기 공통 헬퍼.

순수 함수. 부수효과 없음.
- ``CodeFence``: 펜스 한 구간 (시작/끝/언어/마커)
- ``mark_code_regions(lines)``: 모든 펜스 구간 찾기
- ``is_in_code_region(line_no, fences)``: 특정 줄이 코드 내부인지
- ``strip_inline_code(line)``: 인라인 코드 영역을 공백으로 (위치 보존)
"""

from __future__ import annotations

import re
from dataclasses import dataclass


@dataclass(frozen=True)
class CodeFence:
    """코드펜스 한 구간."""

    start_line: int
    end_line: int | None  # 닫는 줄 (없으면 None for 미닫힌)
    language: str = ""
    marker: str = "```"


_FENCE_OPEN_RE = re.compile(r"^(\s*)(`{3,}|~{3,})(.*)$")


def mark_code_regions(lines: list[str]) -> list[CodeFence]:
    """모든 코드펜스 구간을 list[CodeFence] 로 반환."""
    fences: list[CodeFence] = []
    in_code = False
    open_fence: CodeFence | None = None
    open_marker: str | None = None

    for i, line in enumerate(lines, start=1):
        m = _FENCE_OPEN_RE.match(line)
        if m:
            marker = m.group(2)[:3]  # ``` or ~~~
            if not in_code:
                # 펜스 시작
                language = m.group(3).strip().split()[0] if m.group(3).strip() else ""
                open_fence = CodeFence(
                    start_line=i, end_line=None, language=language, marker=marker
                )
                in_code = True
                open_marker = marker
            elif open_marker and line.lstrip().startswith(open_marker):
                # 펜스 닫기
                if open_fence is not None:
                    fences.append(
                        CodeFence(
                            start_line=open_fence.start_line,
                            end_line=i,
                            language=open_fence.language,
                            marker=open_fence.marker,
                        )
                    )
                in_code = False
                open_fence = None
                open_marker = None

    # 미닫힌 펜스
    if open_fence is not None:
        fences.append(open_fence)

    return fences


def is_in_code_region(line_no: int, fences: list[CodeFence]) -> bool:
    """주어진 줄 번호가 어떤 코드펜스 내부인지."""
    for f in fences:
        if f.end_line is None:
            if line_no > f.start_line:
                return True
        else:
            if f.start_line < line_no < f.end_line:
                return True
    return False


def strip_inline_code(line: str) -> str:
    """인라인 코드(`...`) 영역을 공백으로 치환 (위치 보존).

    코드 외부의 `![...](...)` 또는 `[...](...)` 매칭은 그대로 유지.
    """
    out: list[str] = []
    in_code = False
    for ch in line:
        if ch == "`":
            in_code = not in_code
            out.append(" ")
        elif in_code:
            out.append(" ")
        else:
            out.append(ch)
    return "".join(out)
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_extractors_code_fence.py -v
```

Expected: **6 passed**.

- [ ] **Step 3: 회귀 (61개 그대로)**

```bash
pytest tests/test_extractors_images.py tests/test_extractors_links.py tests/test_cli.py tests/test_broken_images.py tests/test_dead_links.py 2>&1 | tail -3
```

Expected: 51 passed (기존 images + links + broken_images + dead_links + cli, 0.2.0/0.2.1 영향 없음).

- [ ] **Step 4: 커밋**

```bash
git add src/md_doctor/extractors/code_fence.py
git commit -m "feat(extractors): code_fence 공통 헬퍼 구현 (GREEN, 6 passed)"
```

---

## Task 3: `extractors/__init__.py` 갱신

**Files:**
- Modify: `src/md_doctor/extractors/__init__.py`

- [ ] **Step 1: re-export 추가**

기존:
```python
from md_doctor.extractors.images import ImageRef, extract_image_refs
from md_doctor.extractors.links import LinkRef, extract_link_refs

__all__ = [
    "ImageRef",
    "extract_image_refs",
    "LinkRef",
    "extract_link_refs",
]
```

변경:
```python
from md_doctor.extractors.code_fence import (
    CodeFence,
    is_in_code_region,
    mark_code_regions,
    strip_inline_code,
)
from md_doctor.extractors.html import HtmlRef, extract_html_refs
from md_doctor.extractors.images import ImageRef, extract_image_refs
from md_doctor.extractors.links import LinkRef, extract_link_refs

__all__ = [
    "CodeFence",
    "HtmlRef",
    "ImageRef",
    "LinkRef",
    "extract_html_refs",
    "extract_image_refs",
    "extract_link_refs",
    "is_in_code_region",
    "mark_code_regions",
    "strip_inline_code",
]
```

- [ ] **Step 2: 커밋**

```bash
git add src/md_doctor/extractors/__init__.py
git commit -m "feat(extractors): code_fence + html re-export 추가"
```

---

## Task 4: `extractors/html.py` 실패 테스트 (RED)

**Files:**
- Create: `tests/test_extractors_html.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
"""extractors.html 단위 테스트."""

from __future__ import annotations

from md_doctor.extractors.code_fence import mark_code_regions
from md_doctor.extractors.html import HtmlRef, extract_html_refs


def test_extract_html_refs_self_closing_img():
    """<img src="x.png" alt="foo" /> 추출."""
    text = '<img src="x.png" alt="foo" />'
    lines = text.splitlines()
    refs = extract_html_refs(text, lines, mark_code_regions(lines))
    assert len(refs) == 1
    assert refs[0].tag == "img"
    assert refs[0].attrs["src"] == "x.png"
    assert refs[0].attrs["alt"] == "foo"
    assert refs[0].kind == "inline"


def test_extract_html_refs_paired_anchor():
    """<a href="x.md">text</a> 추출."""
    text = '<a href="x.md">click</a>'
    lines = text.splitlines()
    refs = extract_html_refs(text, lines, mark_code_regions(lines))
    assert len(refs) == 1
    assert refs[0].tag == "a"
    assert refs[0].attrs["href"] == "x.md"


def test_extract_html_refs_block_div():
    """<div>content</div> 추출 (kind=block)."""
    text = "<div>hello</div>"
    lines = text.splitlines()
    refs = extract_html_refs(text, lines, mark_code_regions(lines))
    assert len(refs) == 1
    assert refs[0].tag == "div"
    assert refs[0].kind == "block"


def test_extract_html_refs_br():
    """<br> 추출."""
    text = "line<br>break"
    lines = text.splitlines()
    refs = extract_html_refs(text, lines, mark_code_regions(lines))
    assert len(refs) == 1
    assert refs[0].tag == "br"


def test_extract_html_refs_ignores_code_fence():
    """코드펜스 내부 HTML 무시."""
    text = '```\n<img src="x.png">\n```\n'
    lines = text.splitlines()
    refs = extract_html_refs(text, lines, mark_code_regions(lines))
    assert refs == []


def test_extract_html_refs_ignores_inline_code():
    """인라인 코드 내부 HTML 무시."""
    text = "use `<img src=\"x\">` for examples"
    lines = text.splitlines()
    refs = extract_html_refs(text, lines, mark_code_regions(lines))
    assert refs == []


def test_extract_html_refs_multiple():
    """여러 태그 추출."""
    text = '<img src="a.png"> <a href="b.md">b</a> <div>x</div>'
    lines = text.splitlines()
    refs = extract_html_refs(text, lines, mark_code_regions(lines))
    assert len(refs) == 3
    assert {r.tag for r in refs} == {"img", "a", "div"}


def test_extract_html_refs_line_col():
    """line, col 정확."""
    text = 'para\n<img src="x.png">\n'
    lines = text.splitlines()
    refs = extract_html_refs(text, lines, mark_code_regions(lines))
    assert len(refs) == 1
    assert refs[0].line == 2
    assert refs[0].col == 1
```

- [ ] **Step 2: RED 확인**

```bash
pytest tests/test_extractors_html.py -v
```

Expected: `ModuleNotFoundError: No module named 'md_doctor.extractors.html'`.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_extractors_html.py
git commit -m "test(extractors): html 추출 실패 테스트 추가 (RED)"
```

---

## Task 5: `extractors/html.py` 구현 (GREEN)

**Files:**
- Create: `src/md_doctor/extractors/html.py`

- [ ] **Step 1: 모듈 작성**

```python
"""HTML 태그 추출기 — 마크다운 본문에서 raw HTML 을 파싱.

순수 함수. 부수효과 없음.
- 자체닫힘/쌍 태그 모두 지원
- 코드펜스/인라인 코드 내부 제외 (fences 인자)
- v0.3.0: <img>, <a>, <div>, <span>, <p>, <br> 등 일반 태그
"""

from __future__ import annotations

import re
from collections.abc import Iterator
from dataclasses import dataclass

from md_doctor.extractors.code_fence import is_in_code_region


@dataclass(frozen=True)
class HtmlRef:
    """HTML 태그 한 개."""

    tag: str
    line: int
    col: int
    attrs: dict[str, str]
    kind: str = "inline"  # "inline" (void/paired self-line) | "block" (paired multiline)


# 자체닫힘 또는 일반 태그 시작/끝: <tag ...> or <tag ... />
_TAG_RE = re.compile(
    r"<([a-zA-Z][a-zA-Z0-9]*)\b([^>]*?)/?>", re.DOTALL
)

# 쌍 태그: <tag ...>content</tag>
_PAIRED_RE = re.compile(
    r"<([a-zA-Z][a-zA-Z0-9]*)\b([^>]*?)>(.*?)</\1>", re.DOTALL
)

_ATTR_RE = re.compile(r'([a-zA-Z][a-zA-Z0-9_-]*)\s*=\s*"([^"]*)"')


def _parse_attrs(attr_str: str) -> dict[str, str]:
    """속성 문자열을 dict 로."""
    return {m.group(1): m.group(2) for m in _ATTR_RE.finditer(attr_str)}


def _is_block_tag(tag: str) -> bool:
    """블록 수준 태그 (개행跨 가능)."""
    return tag in {"div", "p", "section", "article", "pre", "blockquote"}


def _iter_all_tags(text: str) -> Iterator[tuple[str, str, int, int]]:
    """모든 태그 (자체닫힘 + 쌍의 시작) 를 (tag, attr_str, line, col) 로 yield.

    쌍 태그의 경우 ``_PAIRED_RE`` 가 처리하므로 여기선 자체닫힘 + 쌍의 시작만.
    """
    for m in _TAG_RE.finditer(text):
        tag = m.group(1).lower()
        attr_str = m.group(2)
        # 위치는 text 전체 기준. 호출자가 lines 로 line 번호 변환.
        offset = m.start()
        line, col = _offset_to_line_col(text, offset)
        yield tag, attr_str, line, col


def _offset_to_line_col(text: str, offset: int) -> tuple[int, int]:
    """text 의 offset → (1-based line, 1-based col)."""
    line = text.count("\n", 0, offset) + 1
    last_nl = text.rfind("\n", 0, offset)
    col = offset - last_nl if last_nl >= 0 else offset + 1
    return line, col


def extract_html_refs(
    text: str,
    lines: list[str],
    fences,  # list[CodeFence]
) -> list[HtmlRef]:
    """마크다운에서 HTML 태그를 추출. 코드 영역 제외.

    Parameters
    ----------
    text:
        원본 마크다운.
    lines:
        ``text.splitlines()`` 와 동등.
    fences:
        ``mark_code_regions(lines)`` 결과. 코드 영역 판별용.
    """
    refs: list[HtmlRef] = []

    # 1. 쌍 태그 먼저 (multi-line 가능)
    seen_offsets: set[int] = set()
    for m in _PAIRED_RE.finditer(text):
        tag = m.group(1).lower()
        attr_str = m.group(2)
        start_offset = m.start()
        line, col = _offset_to_line_col(text, start_offset)
        if is_in_code_region(line, fences):
            continue
        # 인라인 코드 내부도 스킵 (간이: _strip_inline_code 적용)
        masked_line = _strip_inline_code_for_line(lines[line - 1] if line - 1 < len(lines) else "")
        if "<" in masked_line:
            original_idx = masked_line.find("<")
            if original_idx >= 0 and (col - 1) < original_idx:
                continue
        refs.append(
            HtmlRef(
                tag=tag,
                line=line,
                col=col,
                attrs=_parse_attrs(attr_str),
                kind="block" if _is_block_tag(tag) else "inline",
            )
        )
        seen_offsets.add(start_offset)

    # 2. 자체닫힘 또는 쌍의 시작 (단일 라인)
    for tag, attr_str, line, col in _iter_all_tags(text):
        # 이미 쌍 태그로 잡힌 offset 은 스킵
        # (간이: 모든 태그 매치를 처리, _PAIRED_RE 와 _TAG_RE 가 각각 시작 태그를 잡지만
        #  _PAIRED_RE 의 시작은 _TAG_RE 와 별개. 안전을 위해 line+col 기준 dedupe)
        if (line, col, tag) in {(r.line, r.col, r.tag) for r in refs}:
            continue
        if is_in_code_region(line, fences):
            continue
        refs.append(
            HtmlRef(
                tag=tag,
                line=line,
                col=col,
                attrs=_parse_attrs(attr_str),
                kind="inline",
            )
        )

    refs.sort(key=lambda r: (r.line, r.col))
    return refs


def _strip_inline_code_for_line(line: str) -> str:
    """인라인 코드 영역을 공백으로 (위치 보존)."""
    out: list[str] = []
    in_code = False
    for ch in line:
        if ch == "`":
            in_code = not in_code
            out.append(" ")
        elif in_code:
            out.append(" ")
        else:
            out.append(ch)
    return "".join(out)
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_extractors_html.py -v
```

Expected: **8 passed**.

- [ ] **Step 3: 회귀 (code_fence 변경 영향 없음)**

```bash
pytest tests/test_extractors_code_fence.py 2>&1 | tail -3
```

- [ ] **Step 4: 커밋**

```bash
git add src/md_doctor/extractors/html.py
git commit -m "feat(extractors): HtmlRef + extract_html_refs 구현 (GREEN, 8 passed)"
```

---

## Task 6: `extractors/images.py` 리팩토링 (헬퍼 위임)

**Files:**
- Modify: `src/md_doctor/extractors/images.py`

- [ ] **Step 1: 위임 추가**

기존 `_strip_inline_code` 함수를 `code_fence.strip_inline_code` 로 위임. 동작은 100% 동일.

```python
# images.py 상단에 import 추가
from md_doctor.extractors.code_fence import strip_inline_code as _strip_inline_code  # noqa: F401
```

기존 `_strip_inline_code` 함수 정의를 제거. (호환성 위해 alias 유지)

- [ ] **Step 2: 회귀**

```bash
pytest tests/test_extractors_images.py 2>&1 | tail -3
```

Expected: 11 passed (기존 동작 보존).

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/extractors/images.py
git commit -m "refactor(extractors): images _strip_inline_code 위임 (동작 보존)"
```

---

## Task 7: `extractors/links.py` 리팩토링 (헬퍼 위임)

**Files:**
- Modify: `src/md_doctor/extractors/links.py`

- [ ] **Step 1: 위임**

기존의 `_strip_inline_code` 함수를 `code_fence.strip_inline_code` 로 위임.

```python
# links.py 상단
from md_doctor.extractors.code_fence import strip_inline_code as _strip_inline_code
```

기존 함수 정의 제거.

- [ ] **Step 2: 회귀**

```bash
pytest tests/test_extractors_links.py 2>&1 | tail -3
```

Expected: 10 passed.

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/extractors/links.py
git commit -m "refactor(extractors): links _strip_inline_code 위임 (동작 보존)"
```

---

## Task 8: `BrokenImagesCheck` HTML 확장 (BI002) — RED

**Files:**
- Modify: `tests/test_broken_images.py` (append)

- [ ] **Step 1: 실패 테스트 추가**

```python
# BI002: HTML <img src>


def test_html_img_missing_emits_bi002(tmp_path: Path):
    md = tmp_path / "doc.md"
    md.write_text('<img src="missing.png" alt="x">\n', encoding="utf-8")
    fr = diagnose_file(md)
    bi002 = [d for d in fr.diagnoses if d.rule == "BI002"]
    assert len(bi002) == 1
    assert bi002[0].severity is Severity.ERROR
    assert bi002[0].line == 1


def test_html_img_existing_no_diagnosis(tmp_path: Path):
    img = tmp_path / "ok.png"
    img.write_bytes(b"\x89PNG\r\n\x1a\n")
    md = tmp_path / "doc.md"
    md.write_text(f'<img src="{img.name}" alt="x">\n', encoding="utf-8")
    fr = diagnose_file(md)
    bi = [d for d in fr.diagnoses if d.check == "broken-images"]
    assert bi == []
```

- [ ] **Step 2: RED 확인**

```bash
pytest tests/test_broken_images.py -v -k "bi002 or html"
```

Expected: 2 failed (체크가 HTML 처리 안 함).

- [ ] **Step 3: 커밋**

```bash
git add tests/test_broken_images.py
git commit -m "test(broken-images): HTML img (BI002) 실패 테스트 추가 (RED)"
```

---

## Task 9: `BrokenImagesCheck` HTML 확장 (GREEN)

**Files:**
- Modify: `src/md_doctor/checks/broken_images.py`

- [ ] **Step 1: HTML 추출 통합**

```python
# broken_images.py 상단에 import 추가
from md_doctor.extractors.code_fence import mark_code_regions
from md_doctor.extractors.html import extract_html_refs


# _check_local, _check_remote 는 그대로 (target 받음).
# HTML 추출을 위해 ImageRef 와 동일한 인터페이스 wrapper.


def _html_refs_to_image_refs(html_refs):
    """HtmlRef(tag='img') → ImageRef 변환."""
    from md_doctor.extractors.images import ImageRef
    return [
        ImageRef(
            target=ref.attrs.get("src", ""),
            line=ref.line,
            col=ref.col,
            alt=ref.attrs.get("alt", ""),
        )
        for ref in html_refs
        if ref.tag == "img" and "src" in ref.attrs
    ]
```

그리고 `run()` 메서드에 HTML 추출 추가:

```python
def run(self, context):
    path: Path = context["path"]
    base_dir = path.parent
    fences = mark_code_regions(context["lines"])
    
    # 1. 마크다운 ![](...)
    md_refs = extract_image_refs(context["text"], context["lines"])
    for ref in md_refs:
        yield from self._process_ref(base_dir, ref, rule="BI001")
    
    # 2. HTML <img src>
    html_refs = extract_html_refs(context["text"], context["lines"], fences)
    for ref in _html_refs_to_image_refs(html_refs):
        yield from self._process_ref(base_dir, ref, rule="BI002")


def _process_ref(self, base_dir, ref, rule):
    try:
        target = _resolve_path(base_dir, ref.target)
    except (ValueError, OSError) as e:
        yield Diagnosis(
            check=self.name, severity=Severity.ERROR,
            message=f"깨진 이미지 참조: 경로 해석 실패 '{ref.target}': {e}",
            line=ref.line, rule=rule,
        )
        return
    if not target.exists():
        yield Diagnosis(
            check=self.name, severity=Severity.ERROR,
            message=f"깨진 이미지 참조: '{ref.target}' (해상: {target})",
            line=ref.line, rule=rule,
        )
        return
    if target.is_dir():
        yield Diagnosis(
            check=self.name, severity=Severity.ERROR,
            message=f"깨진 이미지 참조: '{ref.target}' 은(는) 디렉터리입니다",
            line=ref.line, rule=rule,
        )
```

기존의 `run()` 직접 로직을 `_process_ref()` 로 추출.

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_broken_images.py -v
```

Expected: **10 passed** (기존 8 + 신규 2).

- [ ] **Step 3: 회귀 (images 추출기 동작 보존)**

```bash
pytest tests/test_extractors_images.py 2>&1 | tail -3
```

- [ ] **Step 4: 커밋**

```bash
git add src/md_doctor/checks/broken_images.py
git commit -m "feat(broken-images): HTML <img src> 추출 추가 (BI002, GREEN)"
```

---

## Task 10: `DeadLinksCheck` HTML 확장 (DL002) — RED

**Files:**
- Modify: `tests/test_dead_links.py` (append)

- [ ] **Step 1: 실패 테스트 추가**

```python
# DL002: HTML <a href>


def test_html_anchor_dead_emits_dl002(tmp_path: Path, mock_head_404):
    md = tmp_path / "doc.md"
    md.write_text('<a href="https://missing.example.com">x</a>\n', encoding="utf-8")
    fr = diagnose_file(md)
    dl002 = [d for d in fr.diagnoses if d.rule == "DL002"]
    assert len(dl002) == 1
    assert dl002[0].severity is Severity.ERROR
    assert "404" in dl002[0].message


def test_html_anchor_existing_no_diagnosis(tmp_path: Path, mock_head_ok):
    md = tmp_path / "doc.md"
    md.write_text('<a href="https://example.com">x</a>\n', encoding="utf-8")
    fr = diagnose_file(md)
    dl = [d for d in fr.diagnoses if d.check == "dead-links"]
    assert dl == []
```

- [ ] **Step 2: RED 확인**

```bash
pytest tests/test_dead_links.py -v -k "dl002 or html"
```

Expected: 2 failed.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_dead_links.py
git commit -m "test(dead-links): HTML anchor (DL002) 실패 테스트 추가 (RED)"
```

---

## Task 11: `DeadLinksCheck` HTML 확장 (GREEN)

**Files:**
- Modify: `src/md_doctor/checks/dead_links.py`

- [ ] **Step 1: HTML 통합**

```python
# dead_links.py 상단
from md_doctor.extractors.code_fence import mark_code_regions
from md_doctor.extractors.html import extract_html_refs


def _html_refs_to_link_refs(html_refs):
    """HtmlRef(tag='a') → LinkRef 변환."""
    from md_doctor.extractors.links import LinkRef
    return [
        LinkRef(
            target=ref.attrs.get("href", ""),
            line=ref.line,
            col=ref.col,
            text="",  # 쌍 태그의 text 는 _PAIRED_RE 가 추출 안 함 (단순화)
            kind="autolink",  # 일관성
        )
        for ref in html_refs
        if ref.tag == "a" and "href" in ref.attrs
    ]
```

`run()` 메서드:

```python
def run(self, context):
    path: Path = context["path"]
    base_dir = path.parent
    fences = mark_code_regions(context["lines"])
    
    md_refs = extract_link_refs(context["text"], context["lines"])
    html_refs = extract_html_refs(context["text"], context["lines"], fences)
    all_refs = list(md_refs) + _html_refs_to_link_refs(html_refs)
    
    for ref in all_refs:
        if ref.kind == "definition":
            continue
        # HTML 추출은 마크다운이 아니므로 "definition" 이나 "shortcut" 등이 아님
        if _is_local_path(ref.target):
            yield from _check_local(base_dir, ref, rule="DL001")
        else:
            yield from _check_remote(ref, rule="DL001")
```

`_check_local`, `_check_remote` 의 시그니처에 `rule` 파라미터 추가. 호출 시 "DL001" 또는 "DL002" 전달.

**구현 디테일**: 기존 `_check_local(base_dir, ref)` 와 호환을 위해 rule 인자를 optional 로 만들고 default="DL001".

```python
def _check_local(base_dir, ref, rule="DL001"):
    ...
    yield Diagnosis(..., rule=rule)


def _check_remote(ref, rule="DL001"):
    ...
    yield Diagnosis(..., rule=rule)
```

HTML `href` 의 경우 `_check_remote`/`_check_local` 의 `rule="DL002"`.

```python
for ref in all_refs:
    if ref.kind == "definition":
        continue
    is_html = any(r.line == ref.line and r.col == ref.col for r in html_refs)
    rule = "DL002" if is_html else "DL001"
    if _is_local_path(ref.target):
        yield from _check_local(base_dir, ref, rule=rule)
    else:
        yield from _check_remote(ref, rule=rule)
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_dead_links.py -v
```

Expected: **10 passed** (기존 8 + 신규 2).

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/checks/dead_links.py
git commit -m "feat(dead-links): HTML <a href> 추출 추가 (DL002, GREEN)"
```

---

## Task 12: `GfmLintCheck` 실패 테스트 (RED)

**Files:**
- Create: `tests/test_gfm_lint.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
"""GfmLintCheck 단위 테스트 — HC005~008 4개 규칙."""

from __future__ import annotations

from pathlib import Path

from md_doctor import Severity
from md_doctor.core import diagnose_file


def test_clean_markdown_no_diagnosis(tmp_path: Path):
    md = tmp_path / "clean.md"
    md.write_text(
        "# Title\n\n- item 1\n- item 2\n\n```python\nprint()\n```\n",
        encoding="utf-8",
    )
    fr = diagnose_file(md)
    assert all(d.check != "gfm-lint" for d in fr.diagnoses)


def test_hc005_mixed_bullets_warning(tmp_path: Path):
    md = tmp_path / "mixed.md"
    md.write_text("- a\n* b\n+ c\n", encoding="utf-8")
    fr = diagnose_file(md)
    hc005 = [d for d in fr.diagnoses if d.rule == "HC005"]
    assert len(hc005) == 1
    assert hc005[0].severity is Severity.WARNING


def test_hc005_mixed_ordered_warning(tmp_path: Path):
    md = tmp_path / "ordered.md"
    md.write_text("1. a\n2) b\n", encoding="utf-8")
    fr = diagnose_file(md)
    hc005 = [d for d in fr.diagnoses if d.rule == "HC005"]
    assert any("." in d.message and ")" in d.message for d in hc005)


def test_hc005_consistent_no_warning(tmp_path: Path):
    md = tmp_path / "ok.md"
    md.write_text("- a\n- b\n1. c\n2. d\n", encoding="utf-8")
    fr = diagnose_file(md)
    assert all(d.rule != "HC005" for d in fr.diagnoses)


def test_hc006_code_no_language_info(tmp_path: Path):
    md = tmp_path / "code.md"
    md.write_text("```\ncode\n```\n", encoding="utf-8")
    fr = diagnose_file(md)
    hc006 = [d for d in fr.diagnoses if d.rule == "HC006"]
    assert len(hc006) == 1
    assert hc006[0].severity is Severity.INFO


def test_hc006_code_with_language_no_diagnosis(tmp_path: Path):
    md = tmp_path / "code.md"
    md.write_text("```python\nprint()\n```\n", encoding="utf-8")
    fr = diagnose_file(md)
    assert all(d.rule != "HC006" for d in fr.diagnoses)


def test_hc007_raw_html_warning(tmp_path: Path):
    md = tmp_path / "raw.md"
    md.write_text("<div>content</div>\n", encoding="utf-8")
    fr = diagnose_file(md)
    hc007 = [d for d in fr.diagnoses if d.rule == "HC007"]
    assert len(hc007) == 1
    assert hc007[0].severity is Severity.WARNING
    assert "div" in hc007[0].message


def test_hc007_gfm_compatible_no_warning(tmp_path: Path):
    md = tmp_path / "gfm.md"
    md.write_text("<br>\n<sub>2</sub>\n", encoding="utf-8")
    fr = diagnose_file(md)
    assert all(d.rule != "HC007" for d in fr.diagnoses)


def test_hc008_misaligned_table_warning(tmp_path: Path):
    md = tmp_path / "table.md"
    md.write_text("| a | b | c |\n|---|---|\n| 1 | 2 | 3 |\n", encoding="utf-8")
    fr = diagnose_file(md)
    hc008 = [d for d in fr.diagnoses if d.rule == "HC008"]
    assert len(hc008) == 1
    assert hc008[0].severity is Severity.WARNING


def test_hc008_aligned_table_no_warning(tmp_path: Path):
    md = tmp_path / "table.md"
    md.write_text(
        "| a | b | c |\n|---|---|---|\n| 1 | 2 | 3 |\n",
        encoding="utf-8",
    )
    fr = diagnose_file(md)
    assert all(d.rule != "HC008" for d in fr.diagnoses)


def test_gfm_lint_in_code_fence_ignored(tmp_path: Path):
    md = tmp_path / "code.md"
    md.write_text("```\n- a\n* b\n<div>x</div>\n```\n", encoding="utf-8")
    fr = diagnose_file(md)
    assert all(d.check != "gfm-lint" for d in fr.diagnoses)
```

- [ ] **Step 2: RED 확인**

```bash
pytest tests/test_gfm_lint.py -v
```

Expected: 체크가 없으므로 진단 0 → 다수 fail.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_gfm_lint.py
git commit -m "test(gfm-lint): 4 규칙 실패 테스트 추가 (RED)"
```

---

## Task 13: `GfmLintCheck` 구현 (GREEN)

**Files:**
- Create: `src/md_doctor/checks/gfm_lint.py`

- [ ] **Step 1: 체크 작성**

```python
"""GFM/CommonMark 규칙 린트 — HC005~008 4개 규칙 단일 체크."""

from __future__ import annotations

import re
from collections.abc import Iterator
from typing import Any

from md_doctor.checks import BaseCheck
from md_doctor.extractors.code_fence import mark_code_regions
from md_doctor.extractors.html import extract_html_refs
from md_doctor.models import Diagnosis, Severity


# GFM-호환 HTML (HC007 면제)
_GFM_ALLOWED_HTML = frozenset({"br", "kbd", "sub", "sup", "details", "summary"})

# 리스트 글머리: - * +
_BULLET_RE = re.compile(r"^(\s*)([-*+])(\s|$)")
# 순서 리스트: 1. or 1)
_ORDERED_RE = re.compile(r"^(\s*)(\d+)([.)])(\s|$)")

# 테이블 separator: |---|:---:| 등
_SEPARATOR_RE = re.compile(r"^\s*\|?\s*:?-+:?\s*(\|\s*:?-+:?\s*)+\|?\s*$")


class GfmLintCheck(BaseCheck):
    """GFM/CommonMark 규칙 린트."""

    name = "gfm-lint"

    def run(self, context: dict[str, Any]) -> Iterator[Diagnosis]:
        text: str = context["text"]
        lines: list[str] = context["lines"]
        fences = mark_code_regions(lines)

        yield from _check_hc005(lines, fences)
        yield from _check_hc006(fences)
        yield from _check_hc007(text, lines, fences)
        yield from _check_hc008(lines, fences)


def _check_hc005(lines, fences) -> Iterator[Diagnosis]:
    """HC005: 리스트 점 패턴 일관성."""
    bullets: set[str] = set()
    ordered_marks: set[str] = set()
    
    for i, line in enumerate(lines, start=1):
        if _is_in_fence(i, fences):
            continue
        m_b = _BULLET_RE.match(line)
        m_o = _ORDERED_RE.match(line)
        if m_b:
            bullets.add(m_b.group(2))
        if m_o:
            ordered_marks.add(m_o.group(3))
    
    if len(bullets) > 1:
        yield Diagnosis(
            check="gfm-lint",
            severity=Severity.WARNING,
            message=f"글머리 기호 혼용: {sorted(bullets)}. 하나만 사용 권장.",
            rule="HC005",
        )
    if len(ordered_marks) > 1:
        yield Diagnosis(
            check="gfm-lint",
            severity=Severity.WARNING,
            message=f"순서 리스트 마커 혼용: {sorted(ordered_marks)}. 하나만 사용 권장.",
            rule="HC005",
        )


def _check_hc006(fences) -> Iterator[Diagnosis]:
    """HC006: 코드언어 미지정."""
    for fence in fences:
        if fence.end_line is not None and not fence.language:
            yield Diagnosis(
                check="gfm-lint",
                severity=Severity.INFO,
                message=(
                    f"코드언어 미지정 (라인 {fence.start_line}). "
                    "신택스 하이라이팅 개선을 위해 ```언어 지정 권장."
                ),
                line=fence.start_line,
                rule="HC006",
            )


def _check_hc007(text, lines, fences) -> Iterator[Diagnosis]:
    """HC007: raw HTML 사용."""
    for ref in extract_html_refs(text, lines, fences):
        if ref.tag in _GFM_ALLOWED_HTML:
            continue
        yield Diagnosis(
            check="gfm-lint",
            severity=Severity.WARNING,
            message=(
                f"raw HTML <{ref.tag}> 사용 (라인 {ref.line}). "
                "마크다운 대체 권장 (호환성)."
            ),
            line=ref.line,
            rule="HC007",
        )


def _check_hc008(lines, fences) -> Iterator[Diagnosis]:
    """HC008: 테이블 정렬."""
    for i, line in enumerate(lines, start=1):
        if _is_in_fence(i, fences):
            continue
        if not _SEPARATOR_RE.match(line):
            continue
        if i < 2:
            continue
        header_line = lines[i - 2]
        n_header = _count_table_columns(header_line)
        n_sep = _count_table_columns(line)
        if n_header != n_sep:
            yield Diagnosis(
                check="gfm-lint",
                severity=Severity.WARNING,
                message=(
                    f"테이블 정렬 불일치 (라인 {i}): "
                    f"헤더 {n_header}열 vs separator {n_sep}열."
                ),
                line=i,
                rule="HC008",
            )


def _is_in_fence(line_no: int, fences) -> bool:
    """펜스 시작/끝 자체는 검사 대상 (라인이 펜스 boundary 면 스킵)."""
    for f in fences:
        if f.end_line is None:
            if line_no >= f.start_line:
                return True
        else:
            if f.start_line <= line_no <= f.end_line:
                return True
    return False


def _count_table_columns(row: str) -> int:
    """테이블 행의 열 개수 계산."""
    row = row.strip()
    if not row or set(row) <= {"|", " "}:
        return 0
    if row.startswith("|"):
        row = row[1:]
    if row.endswith("|"):
        row = row[:-1]
    return len([c for c in row.split("|") if c.strip()])
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_gfm_lint.py -v
```

Expected: **11 passed**.

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/checks/gfm_lint.py
git commit -m "feat(gfm-lint): GfmLintCheck 구현 (GREEN, 11 passed, HC005-008)"
```

---

## Task 14: `default_checks()` 에 GfmLintCheck 등록

**Files:**
- Modify: `src/md_doctor/checks/__init__.py`

- [ ] **Step 1: 등록**

```python
def default_checks() -> CheckRegistry:
    from md_doctor.checks.broken_images import BrokenImagesCheck
    from md_doctor.checks.dead_links import DeadLinksCheck
    from md_doctor.checks.gfm_lint import GfmLintCheck
    from md_doctor.checks.heading_consistency import HeadingConsistencyCheck
    from md_doctor.checks.token_stats import TokenStatsCheck
    
    return CheckRegistry(
        checks=[
            TokenStatsCheck(),
            HeadingConsistencyCheck(),
            BrokenImagesCheck(),
            DeadLinksCheck(),
            GfmLintCheck(),
        ]
    )
```

- [ ] **Step 2: 전체 회귀**

```bash
pytest 2>&1 | tail -3
```

Expected: **89 passed** (기존 61 + 신규 28). 확인.

- [ ] **Step 3: 커밋**

```bash
git add src/md_doctor/checks/__init__.py
git commit -m "feat(checks): default_checks() 에 GfmLintCheck 등록 (89 passed)"
```

---

## Task 15: 기존 `test_default_checks_have_unique_names` 업데이트 (4 → 5)

**Files:**
- Modify: `tests/test_cli.py`

- [ ] **Step 1: 5개 검사 검증 추가**

```python
def test_default_checks_have_unique_names():
    reg = default_checks()
    names = reg.names()
    assert len(names) == len(set(names)), f"중복 검사 이름: {names}"
    # 0.1.0: 2개. 0.2.0: +broken-images. 0.2.1: +dead-links. 0.3.0: +gfm-lint (5개).
    assert "token-stats" in names
    assert "heading-consistency" in names
    assert "broken-images" in names
    assert "dead-links" in names
    assert "gfm-lint" in names
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_cli.py::test_default_checks_have_unique_names -v
```

- [ ] **Step 3: 커밋**

```bash
git add tests/test_cli.py
git commit -m "test(cli): 기본 레지스트리에 gfm-lint 포함 검증 추가"
```

---

## Task 16: CLI 통합 테스트 2개 (gfm-lint 단독)

**Files:**
- Modify: `tests/test_cli.py` (append)

- [ ] **Step 1: 추가**

```python
# gfm-lint CLI 통합


def test_cli_runs_gfm_lint_check(tmp_path: Path, capsys):
    """`--checks gfm-lint` 로 단독 실행 가능."""
    md = tmp_path / "clean.md"
    md.write_text("# Title\n\n- a\n- b\n", encoding="utf-8")
    rc = main([str(md), "--checks", "gfm-lint", "--format", "text"])
    assert rc == 0


def test_cli_fails_on_gfm_lint_warning(tmp_path: Path, capsys):
    """gfm-lint WARNING 발견 시 exit 1."""
    md = tmp_path / "bad.md"
    md.write_text("- a\n* b\n", encoding="utf-8")
    rc = main([str(md), "--format", "text"])
    assert rc == 1
    out = capsys.readouterr().out
    assert "[HC005]" in out
```

- [ ] **Step 2: GREEN 확인**

```bash
pytest tests/test_cli.py -v -k "gfm"
```

Expected: 2 passed.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_cli.py
git commit -m "test(cli): gfm-lint CLI 통합 테스트 2개 추가"
```

---

## Task 17: 수동 QA

**Files:** (없음 — 수동 실행)

- [ ] **Step 1: 깨끗한 README**

```bash
python -m md_doctor README.md --checks gfm-lint --format text 2>&1 | head -20
```

Expected: README 자체에 HC005-008 위반 없음 (0 issues).

- [ ] **Step 2: 의도적 위반 시나리오**

```bash
cat > /tmp/qa_gfm.md <<'EOF'
# QA

- item 1
* item 2
+ item 3

1. ordered a
2) ordered b

```
raw code
```

| a | b | c |
|---|---|

<iframe src="evil"></iframe>
EOF

python -m md_doctor /tmp/qa_gfm.md --format text
```

Expected: HC005×2 (글머리 혼용, 순서 혼용) + HC008 (테이블 정렬) + HC007 (iframe).

- [ ] **Step 3: HTML 이미지/링크**

```bash
cat > /tmp/qa_html.md <<'EOF'
# HTML

<img src="missing.png" alt="x">

<a href="https://missing.example.com">dead</a>
EOF

python -m md_doctor /tmp/qa_html.md --format text 2>&1
```

Expected: HC007 (img/a), BI002 (img 깨짐), DL002 (link 404). mock 없이 실제 URL → 404.

- [ ] **Step 4: GitHub 포맷**

```bash
python -m md_doctor /tmp/qa_gfm.md --format github --min-severity warning
```

Expected: `::warning file=...` × 다수.

- [ ] **Step 5: 임시 파일 정리**

```bash
rm -f /tmp/qa_gfm.md /tmp/qa_html.md
```

---

## Task 18: CHANGELOG + README + version 갱신

**Files:**
- Modify: `CHANGELOG.md`, `README.md`, `pyproject.toml`, `src/md_doctor/__init__.py`, `tests/test_cli.py`

- [ ] **Step 1: CHANGELOG.md v0.3.0 섹션 추가 (top)**

```markdown
## [0.3.0] - 2026-06-09

### Added
- 🆕 `gfm-lint` check (HC005~HC008) — 리스트 점 일관성, 코드언어 지정, raw HTML, 테이블 정렬
- 🧪 `extractors/code_fence.py` — 코드펜스 + 인라인 코드 마킹 공통 헬퍼
- 🧪 `extractors/html.py` — HTML 태그 추출 (<img>, <a>, <div> 등)
- 🔗 `BrokenImagesCheck` HTML 확장 (BI002) — `<img src>` 추출
- 🔗 `DeadLinksCheck` HTML 확장 (DL002) — `<a href>` 추출
- ✅ 28 신규 테스트 (code_fence 6, html 8, gfm-lint 11, broken/dead +2×2, cli +2), 총 89개

### Changed
- 기본 레지스트리: 4 → 5 활성 검사 (+gfm-lint)
- `extractors/images.py`/`links.py` 가 `code_fence.strip_inline_code` 위임 (동작 100% 보존)

### Notes
- GFM/CommonMark 핵심 규칙 4개 (HC005-008) 만 포함. 작업 목록, 각주 등은 v0.3.x 후속.
- HTML 추출은 정규식 기반. 정밀 파서는 v0.4.0+ 후보.
- zero-deps 정책 유지.

## [0.2.1] - 2026-06-09
```

- [ ] **Step 2: README 검사 모듈 표에 gfm-lint 추가**

기존:
```
| **dead-links** | DL001 | ERROR | ✅ 0.2.1 | 외부/내부 링크 HEAD (5s 타임아웃) |
```

변경:
```
| **dead-links** | DL001 | ERROR | ✅ 0.2.1 | 외부/내부 링크 HEAD (5s 타임아웃) |
| | DL002 | ERROR | ✅ 0.3.0 | `<a href>` HTML 추출 |
| **broken-images** | BI001 | ERROR | ✅ 0.2.0 | `![](...)` 참조의 유효성 (로컬) |
| | BI002 | ERROR | ✅ 0.3.0 | `<img src>` HTML 추출 |
| **gfm-lint** | HC005-HC008 | WARNING/INFO | ✅ 0.3.0 | GFM/CommonMark 규칙 |
```

- [ ] **Step 3: README 비고 + 로드맵 갱신**

비고:
```
> **0.1.0**: token-stats + heading-consistency 활성.
> **0.2.0**: + broken-images (로컬) 활성.
> **0.2.1**: + dead-links (로컬 + URL HEAD) 활성.
> **0.3.0**: + gfm-lint (HC005-008) + HTML <img>/<a> 추출.
```

로드맵:
```
- **0.2.1** ✅ — dead-links (로컬 + URL HEAD) 활성화
- **0.3.0** ✅ — GFM/CommonMark 린트 (gfm-lint + HTML 추출)
```

- [ ] **Step 4: version bump**

`pyproject.toml`: `version = "0.2.1"` → `0.3.0`
`src/md_doctor/__init__.py`: `__version__ = "0.2.1"` → `0.3.0`
`tests/test_cli.py`: 3개 위치 (`__version__`, `md-doctor v0.2.1`, `"version": "0.2.1"`) → `0.3.0`

- [ ] **Step 5: 회귀 + 커밋**

```bash
pytest 2>&1 | tail -3
git add CHANGELOG.md README.md pyproject.toml src/md_doctor/__init__.py tests/test_cli.py
git commit -m "docs: v0.3.0 CHANGELOG/README 갱신 + version bump (0.2.1 → 0.3.0)"
```

---

## Task 19: 최종 검증

- [ ] **Step 1: 전체 테스트**

```bash
pytest -v 2>&1 | tail -10
```

Expected: **89 passed**.

- [ ] **Step 2: ruff 신규 파일**

```bash
ruff check src/md_doctor/extractors/code_fence.py src/md_doctor/extractors/html.py src/md_doctor/checks/gfm_lint.py tests/test_extractors_code_fence.py tests/test_extractors_html.py tests/test_gfm_lint.py
```

- [ ] **Step 3: zero-deps**

```bash
grep -A 1 "^dependencies" pyproject.toml
```

Expected: `dependencies = []`.

- [ ] **Step 4: CLI sanity**

```bash
python -m md_doctor --list-checks
python -m md_doctor --version
```

Expected: 5 모듈, v0.3.0.

- [ ] **Step 5: git status**

```bash
git status
```

Expected: working tree clean.

---

## 변경 요약 (전체)

| Task | 신규 | 수정 | 테스트 추가 |
|------|------|------|------------|
| 1-2 | 1 (test) + 1 (impl) | - | +6 |
| 3 | - | 1 (re-export) | 0 |
| 4-5 | 1 (test) + 1 (impl) | - | +8 |
| 6-7 | - | 2 (refactor) | 0 |
| 8-9 | - | 2 (broken_images) | +2 |
| 10-11 | - | 2 (dead_links) | +2 |
| 12-13 | 1 (test) + 1 (impl) | - | +11 |
| 14 | - | 1 (default_checks) | 0 |
| 15-16 | - | 1 (test_cli) | +2 |
| 17 | - | (수동) | 0 |
| 18 | - | 5 (docs+version+test) | 0 |
| 19 | - | (검증) | 0 |

**총 커밋**: 18
**최종 테스트**: 89 passed (기존 61 + 신규 28)

---

## 위험 & 롤백

- **Task 6-7 리팩토링 회귀**: 51개 기존 테스트가 그대로 통과해야 안전. RED 시 즉시 롤백.
- **Task 9/11 HTML 통합**: 쌍 태그 + 자체닫힘 regex 순서가 잘못되면 중복/누락. `seen_offsets` 또는 line+col+tag dedupe 필요.
- **Task 13 HC005**: 1개 파일에 `-` 만 쓰면 `bullets` set 크기 1 → 진단 0. OK.
- **Task 13 HC008 separator 파싱**: `|---|---|` 처럼 col 3개 / `|---|` 처럼 col 1개 케이스 모두 처리.

롤백:
```bash
git log --oneline -20
git reset --hard <last-green-commit>
```
